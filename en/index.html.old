<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1.0" name="viewport"/>
<title>Hollywood: Is the Sequel Model Hitting Its Limit? — Scénario</title>
<link href="https://lesscenarios.fr/en/archives/2026-09-12.html" rel="canonical"/>
<link href="https://lesscenarios.fr/archives/2026-09-12.html" hreflang="fr" rel="alternate"/>
<link href="https://lesscenarios.fr/en/archives/2026-09-12.html" hreflang="en" rel="alternate"/>
<link href="https://lesscenarios.fr/archives/2026-09-12.html" hreflang="x-default" rel="alternate"/>
<link href="../assets/logo.svg" rel="icon" type="image/svg+xml"/>
<link href="../manifest.webmanifest" rel="manifest"/>
<meta content="#10151c" name="theme-color"/>
<link href="../assets/icon-192.png" rel="apple-touch-icon"/>
<meta content="yes" name="apple-mobile-web-app-capable"/>
<meta content="black-translucent" name="apple-mobile-web-app-status-bar-style"/>
<meta content="Scénario" name="apple-mobile-web-app-title"/>
<link href="../assets/pwa-install.css" rel="stylesheet"/>
<meta content="In 2026, several big-budget sequels flopped in theaters while original films thrived: is the franchise bet still as safe?" name="description"/>
<meta content="article" property="og:type"/>
<meta content="Scénario" property="og:site_name"/>
<meta content="en_US" property="og:locale"/>
<meta content="https://lesscenarios.fr/en/" property="og:url"/>
<meta content="Hollywood: Is the Sequel Model Hitting Its Limit? — Scénario" property="og:title"/>
<meta content="In 2026, several big-budget sequels flopped in theaters while original films thrived: is the franchise bet still as safe?" property="og:description"/>
<meta content="https://lesscenarios.fr/en/assets/social/instagram/2026-09-12.png" property="og:image"/>
<meta content="1080" property="og:image:width"/>
<meta content="1080" property="og:image:height"/>
<meta content="Empty movie theater, screen lit up facing rows of red seats." property="og:image:alt"/>
<meta content="Scénario" property="article:author"/>
<meta content="culture-divertissement" name="domain"/>
<meta content="2026-09-12T07:30:00+02:00" property="article:published_time"/>
<meta content="summary_large_image" name="twitter:card"/>
<meta content="Hollywood: Is the Sequel Model Hitting Its Limit? — Scénario" name="twitter:title"/>
<meta content="In 2026, several big-budget sequels flopped in theaters while original films thrived: is the franchise bet still as safe?" name="twitter:description"/>
<meta content="https://lesscenarios.fr/en/assets/social/instagram/2026-09-12.png" name="twitter:image"/>
<script type="application/ld+json">{
  "@context": "https://schema.org",
  "@type": "NewsArticle",
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": "https://lesscenarios.fr/en/"
  },
  "headline": "Hollywood: Is the Sequel Model Hitting Its Limit?",
  "description": "In 2026, several big-budget sequels flopped in theaters while original films thrived: is the franchise bet still as safe?",
  "image": [
    "https://lesscenarios.fr/en/assets/social/instagram/2026-09-12.png"
  ],
  "datePublished": "2026-09-12T07:30:00+02:00",
  "dateModified": "2026-09-12T07:30:00+02:00",
  "inLanguage": "en-US",
  "author": {
    "@type": "Organization",
    "name": "Scénario",
    "url": "https://lesscenarios.fr/le-projet.html",
    "sameAs": [
      "https://www.linkedin.com/company/136694258/"
    ]
  },
  "publisher": {
    "@type": "Organization",
    "name": "Scénario",
    "logo": {
      "@type": "ImageObject",
      "url": "https://lesscenarios.fr/assets/logo-512.png",
      "width": 512,
      "height": 512
    }
  }
}</script>
<link href="https://fonts.googleapis.com" rel="preconnect"/>
<link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect"/>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600;9..144,700&amp;family=Inter:wght@400;500;600&amp;family=JetBrains+Mono:wght@400;500;700&amp;display=swap" rel="stylesheet"/>
<style>
  :root{
    --ink: #10151c;
    --surface: #1a212b;
    --surface-2: #212a35;
    --hairline: #2c3644;
    --paper: #ece7da;
    --paper-dim: #a9a89c;
    --gold: #cf9d4c;
    --favorable: #5e9c78;
    --stable: #6f8fae;
    --degrade: #bd6248;
  }

  *,*::before,*::after{ box-sizing: border-box; }

  html{ scroll-behavior: smooth; }

  body{
    margin:0;
    background: var(--ink);
    color: var(--paper);
    font-family: "Inter", system-ui, sans-serif;
    line-height: 1.55;
    -webkit-font-smoothing: antialiased;
  }

  a{ color: inherit; }

  ::selection{ background: var(--gold); color: var(--ink); }

  :focus-visible{
    outline: 2px solid var(--gold);
    outline-offset: 3px;
  }

  .wrap{
    max-width: 920px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* ---------- Masthead ---------- */

  header.masthead{
    border-bottom: 1px solid var(--hairline);
    padding: 28px 0 22px;
  }

  .masthead-row{
    display:flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    flex-wrap: wrap;
  }

  .wordmark{
    font-family: "Fraunces", serif;
    font-weight: 600;
    font-size: 1.5rem;
    letter-spacing: 0.01em;
  }

  .wordmark span{ color: var(--gold); }

  .brand{
    display: flex;
    flex-direction: column;
    gap: 3px;
  }

  .brand-row{
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    row-gap: 4px;
    gap: 11px;
  }

  /* Accroche du site, ajoutée le 28 août (retour utilisateur : un premier
     visiteur qui atterrit directement sur l'édition du jour — cas le plus
     fréquent, index.html sert à la fois d'accueil et d'article — n'a
     aucun repère pour comprendre ce qu'est Scénario sans cliquer sur
     "Le projet"). Reprend mot pour mot la description officielle du flux
     RSS (<description> du channel, feed.xml) plutôt qu'une nouvelle
     formulation. Dans le bandeau .masthead, donc présente sur toutes les
     pages du site sans exception — peu importe la page d'atterrissage. */
  /* Icône notifications dans le masthead, ajoutée le 21 août — 3e point
     d'entrée vers le même abonnement OneSignal (avec le rappel compact
     après "L'essentiel" et le bloc dédié en bas de page), celui-ci visible
     dès le chargement, avant tout scroll. Icône seule, pas de .btn-label
     (le script gère déjà l'absence de label — voir onesignalSetLabel) :
     le retour d'état passe par l'attribut title (tooltip) à la place. */
  .masthead-right{
    display: flex;
    align-items: center;
    gap: 14px;
  }
  .masthead-notif-btn{
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    flex-shrink: 0;
    padding: 0;
    background: transparent;
    border: 1px solid var(--gold);
    border-radius: 100px;
    color: var(--gold);
    cursor: pointer;
    appearance: none;
    -webkit-appearance: none;
    transition: background .15s, color .15s;
  }
  .masthead-notif-btn:hover{ background: var(--gold); color: var(--ink); }
  .masthead-notif-btn:disabled{ opacity: .5; cursor: default; }
  .masthead-notif-btn svg{ display: block; }

  /* Séparateur entre le groupe "notifications/impression" et le groupe
     "sujet révisé/récap hebdo", ajouté le 29 août quand ces deux derniers
     ont rejoint la même ligne (fusionnés depuis la bande .top-updates,
     retour utilisateur : doit tenir sur une seule ligne y compris en
     mobile). Simple trait vertical fin, purement visuel (aria-hidden). */
  .masthead-divider{
    width: 1px;
    height: 20px;
    background: var(--hairline);
    flex-shrink: 0;
  }

  /* Bouton de bascule de langue dans le masthead, ajouté le 29 août —
     retour utilisateur : il manquait un moyen évident de passer du
     français à l'anglais et inversement. Même recette visuelle que
     .masthead-notif-btn (pilule dorée, 32px de haut) mais un libellé
     texte à la place d'une icône : "EN"/"FR" se comprend sans légende.
     Présent uniquement sur les pages qui ont un équivalent dans l'autre
     langue — jamais un lien générique vers l'accueil de l'autre langue,
     voir docs/routine-en-prompt.md et docs/routine-prompt.md pour la
     règle d'ajout/retrait. */
  .masthead-lang-btn{
    display: flex;
    align-items: center;
    justify-content: center;
    height: 32px;
    padding: 0 13px;
    flex-shrink: 0;
    background: transparent;
    border: 1px solid var(--gold);
    border-radius: 100px;
    color: var(--gold);
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    text-decoration: none;
    transition: background .15s, color .15s;
  }
  .masthead-lang-btn:hover{ background: var(--gold); color: var(--ink); }

  .brand-mark{
    width: 30px;
    height: 30px;
    display: block;
    flex: none;
  }

  .edition{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.74rem;
    color: var(--paper-dim);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .masthead-nav{
    display: flex;
    align-items: baseline;
    gap: 20px;
    flex-wrap: wrap;
  }

  .nav-link{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--paper-dim);
    text-decoration: none;
    border-bottom: 1px dotted transparent;
  }

  .nav-link:hover{
    color: var(--paper);
    border-bottom-color: var(--paper-dim);
  }

  /* ---------- Top nav bar ---------- */

  nav.topnav{
    background: var(--surface);
    border-bottom: 1px solid var(--hairline);
    position: relative;
  }

  nav.topnav .wrap{
    display: flex;
    gap: 16px;
    padding: 12px 24px;
    overflow-x: auto;
    scrollbar-width: none;
  }

  nav.topnav .wrap::-webkit-scrollbar{ display: none; }

  /* Dégradé de bord, ajouté le 21 août — retour utilisateur : la nav
     scrolle horizontalement sur mobile (elle contient plus d'items que
     l'écran n'en affiche, ex. "Newsletter" reste hors champ) mais sans
     scrollbar visible (`scrollbar-width: none` ci-dessus), donc rien ne
     signale qu'il y a plus à voir. Ce voile ne fait qu'indiquer visuellement
     qu'on peut glisser — aucun nouveau lien ni bloc ajouté. */
  nav.topnav::after{
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    width: 32px;
    background: linear-gradient(90deg, transparent, var(--surface));
    pointer-events: none;
  }

  .topnav a{ flex-shrink: 0; }

  .topnav a{
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.76rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--paper-dim);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    padding-bottom: 2px;
  }

  .topnav a svg{ display: block; flex-shrink: 0; }

  .topnav a:hover{ color: var(--paper); }

  .topnav a[aria-current="page"]{
    color: var(--gold);
    border-bottom-color: var(--gold);
  }

  /* ---------- Bandeau d'accueil (premier passage uniquement) ---------- */
  /* Ajouté le 28 août, retour utilisateur : un premier visiteur qui
     atterrit sur l'édition du jour ne comprend pas ce qu'est Scénario
     sans lire ou cliquer ailleurs — « on l'aime dans les 20 premières
     secondes sinon on zappe à jamais ». Visible une seule fois par
     navigateur (localStorage, voir le script en bas de page), jamais
     revu ensuite — server-rendu avec l'attribut `hidden`, dévoilé par le
     script seulement si le visiteur ne l'a jamais fermé/vu. Reprend le
     code couleur des cartes de scénarios (favorable/stable/dégradé) pour
     se lire comme la signature visuelle du site, pas comme un bandeau
     publicitaire de plus. */
  .intro-banner{
    background: var(--surface);
    border-bottom: 1px solid var(--hairline);
  }

  .intro-banner-inner{
    position: relative;
    padding: 30px 24px 28px;
  }

  .intro-banner-close{
    position: absolute;
    top: 14px;
    right: 24px;
    display: flex;
    background: none;
    border: none;
    padding: 6px;
    color: var(--paper-dim);
    cursor: pointer;
  }
  .intro-banner-close:hover{ color: var(--paper); }

  .intro-banner-body{
    display: flex;
    align-items: flex-start;
    gap: 20px;
  }

  .intro-banner-icon{
    width: 52px;
    height: 52px;
    flex-shrink: 0;
    margin-top: 4px;
  }

  @media (max-width: 480px){
    .intro-banner-icon{ display: none; }
  }

  .intro-banner-lead{
    font-family: "Fraunces", serif;
    font-weight: 600;
    font-size: 1.4rem;
    line-height: 1.32;
    max-width: 34ch;
    margin: 0 0 10px;
  }

  .intro-banner-text{
    font-size: 0.94rem;
    color: var(--paper-dim);
    line-height: 1.55;
    max-width: 58ch;
    margin: 0;
  }

  @media (max-width: 560px){
    .intro-banner-inner{ padding: 24px 20px 22px; padding-right: 46px; }
    .intro-banner-lead{ font-size: 1.18rem; }
  }

  /* ---------- Bandeau "Récap de la semaine" (dimanche) ---------- */
  /* Ajouté le 6 septembre, retour utilisateur : l'icône calendrier discrète
     dans le header (.masthead-notif-btn) ne suffit pas à signaler qu'un
     nouveau récap vient de sortir. Bande fine plutôt qu'un bloc comme
     .intro-banner — annonce ponctuelle, pas une accroche éditoriale.
     data-hebdo porte la date du dernier récap : la routine hebdo met à jour
     ce data-attribute, l'href et le texte de plage de dates en même temps
     que le lien .masthead-notif-btn (voir docs/routine-hebdo-prompt.md).
     Se réaffiche automatiquement à chaque nouveau récap (comparaison de
     date en localStorage, pas un simple "vu une fois" comme .intro-banner)
     et se ferme soit par le bouton, soit en cliquant le lien lui-même —
     dans les deux cas, mémorisé pour ne plus revenir avant le récap suivant. */
  .weekly-banner{
    background: var(--surface);
    border-bottom: 1px solid var(--gold);
  }

  .weekly-banner-inner{
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 12px 24px;
  }

  .weekly-banner-link{
    display: flex;
    align-items: center;
    gap: 10px;
    flex: 1 1 auto;
    min-width: 0;
    color: var(--paper);
    text-decoration: none;
  }

  .weekly-banner-link:hover .weekly-banner-text{ text-decoration: underline; }

  .weekly-banner-icon{
    flex-shrink: 0;
    color: var(--gold);
  }

  .weekly-banner-text{
    font-size: 0.9rem;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .weekly-banner-cta{
    flex-shrink: 0;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.76rem;
    font-weight: 600;
    color: var(--gold);
    white-space: nowrap;
  }

  .weekly-banner-close{
    flex-shrink: 0;
    display: flex;
    background: none;
    border: none;
    padding: 4px;
    color: var(--paper-dim);
    cursor: pointer;
  }
  .weekly-banner-close:hover{ color: var(--paper); }

  @media (max-width: 560px){
    .weekly-banner-cta{ display: none; }
  }

  /* ---------- Hero ---------- */

  .hero{
    padding: 0 0 40px;
    border-bottom: 1px solid var(--hairline);
  }

  .eyebrow{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--gold);
    margin: 0 0 18px;
  }

  .pubdate{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    letter-spacing: 0.02em;
    color: var(--paper-dim);
    margin: 0 0 8px;
  }

  .share-inline{
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 8px;
    margin: 0 0 22px;
  }

  .share-inline-label{
    font-size: 0.8rem;
    color: var(--paper-dim);
    margin-right: 2px;
  }

  .share-inline a,
  .share-inline button{
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 30px;
    height: 30px;
    color: var(--gold);
    background: none;
    border: 1px solid var(--hairline);
    border-radius: 100px;
    padding: 0;
    cursor: pointer;
    text-decoration: none;
    transition: color .15s, border-color .15s;
  }

  .share-inline a svg,
  .share-inline button svg{ width: 15px; height: 15px; }

  .share-inline a:hover,
  .share-inline button:hover{ color: var(--paper); border-color: var(--paper-dim); }

  .share-inline button .share-icon-check{ display: none; }
  .share-inline button.is-copied .share-icon-link{ display: none; }
  .share-inline button.is-copied .share-icon-check{ display: inline-flex; }
  .share-inline button.is-copied{ color: var(--favorable); border-color: var(--favorable); }

  .toc{
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 0 0 24px;
  }

  .toc a{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--paper-dim);
    text-decoration: none;
    border: 1px solid var(--hairline);
    border-radius: 100px;
    padding: 6px 12px;
    transition: color .15s, border-color .15s;
  }

  .toc a:hover{ color: var(--paper); border-color: var(--paper-dim); }

  h1{
    font-family: "Fraunces", serif;
    font-weight: 600;
    font-size: clamp(2.1rem, 5vw, 3.4rem);
    line-height: 1.08;
    letter-spacing: -0.01em;
    margin: 0 0 24px;
    max-width: 14ch;
  }

  .dek{
    font-size: 1.1rem;
    color: var(--paper-dim);
    margin: 0 0 20px;
  }

  .dek:last-of-type{ margin: 0 0 32px; }

  .dek strong{ color: var(--paper); font-weight: 600; }

  /* Liste à puces dans le contexte (pas seulement dans les cartes de
     scénarios) — même motif que .field li plus bas dans ce fichier (tiret
     coloré), pour un passage dense à lire d'un coup d'œil plutôt qu'en
     phrase continue. */
  .dek-list{
    list-style: none;
    margin: 0 0 20px;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .dek-list li{
    font-size: 1.1rem;
    line-height: 1.55;
    color: var(--paper-dim);
    padding-left: 20px;
    position: relative;
  }
  .dek-list li::before{
    content: "–";
    position: absolute;
    left: 0;
    color: var(--gold);
  }
  .dek-list strong{ color: var(--paper); font-weight: 600; }

  .dek a{ color: var(--gold); text-decoration: none; border-bottom: 1px dotted var(--gold); }
  .dek a:hover{ color: var(--paper); border-bottom-color: var(--paper); }

  .lex-ref{
    color: var(--gold);
    text-decoration: none;
    font-size: 0.7em;
    vertical-align: super;
    margin-left: 1px;
  }
  .lex-ref:hover{ color: var(--paper); }

  /* Cover plein écran, refonte du 29 août — retour utilisateur : la page
     manquait de "whoua", d'impact visuel dès l'arrivée. Avant : photo en
     petit format (16/9, largeur de lecture 920px) au milieu du hero,
     avec le titre en texte simple au-dessus. Après : la photo passe en
     plein bleed (100vw, edge-to-edge — cas où il ne faut PAS plafonner
     avec un max-width, contrairement au breakout des cartes de
     scénarios plus bas : voir le commentaire sur .cards, ce cover n'a
     justement pas besoin d'un point de rupture, on veut un vrai edge-to-
     edge quelle que soit la largeur d'écran) et le titre (eyebrow + h1 +
     date) vient en incrustation, ancré en bas de l'image sur un dégradé
     sombre — traitement "couverture de magazine". Logo en filigrane en
     haut à gauche, comme avant. Le sujet de la légende (figcaption)
     redescend sous l'image, à la largeur de lecture habituelle
     (`.wrap`). Scopé à `screen` : `.article-image` reste masqué en
     impression (voir liste `@media print` plus bas), donc aucun impact
     sur le PDF 1 page. */
  .article-image{ margin: 0 0 32px; }
  .article-image-photo-wrap{
    position: relative;
    overflow: hidden;
    width: 100vw;
    margin-left: calc(50% - 50vw);
    margin-right: calc(50% - 50vw);
    height: min(64vh, 600px);
    min-height: 340px;
  }
  .article-image-photo{
    position: absolute; inset: 0;
    width: 100%; height: 100%;
    object-fit: cover;
    display: block;
    animation: cover-reveal 1.2s ease-out both;
  }
  @keyframes cover-reveal{
    from{ opacity: 0; transform: scale(1.045); }
    to{ opacity: 1; transform: scale(1); }
  }
  @media (prefers-reduced-motion: reduce){
    .article-image-photo{ animation: none; }
  }
  .article-image-scrim{
    position: absolute; inset: 0;
    background: linear-gradient(180deg, rgba(16,21,28,.82) 0%, rgba(16,21,28,.16) 26%, rgba(16,21,28,.06) 42%, rgba(16,21,28,.55) 60%, rgba(16,21,28,.9) 80%, rgba(16,21,28,1) 100%);
  }
  .article-image-masthead{
    position: absolute; top: 18px; left: 24px;
    display: flex; align-items: center; gap: 7px;
    z-index: 2;
  }
  .article-image-logo{ width: 33px; height: 33px; display: block; }
  .article-image-wordmark{
    font-family: "Fraunces", serif; font-weight: 600; font-size: clamp(1.15rem, 2.3vw, 1.5rem); color: var(--paper);
  }
  .article-image-wordmark span{ color: var(--gold); }

  .article-image-overlay{
    position: absolute; left: 0; right: 0; bottom: 0;
    z-index: 2;
    padding-bottom: 30px;
  }
  /* L'eyebrow a besoin d'un fond derrière le texte, ajouté le 29 août —
     retour utilisateur : "Samedi, culture" en or sur la photo passait
     inaperçu ("trop ton sur ton"), et une ombre portée ne suffit pas à
     garantir la lisibilité puisque la photo change chaque jour (parfois
     dorée/chaude, comme ici). Première version en pastille ronde façon
     .masthead-notif-btn jugée "pas belle" le même jour (retour
     utilisateur : trop badge d'appli, pas assez distingué) — remplacée
     par le langage visuel déjà utilisé pour les encarts du site
     (.question-box/.stakes-box/.comprendre-box : filet doré à gauche,
     radius 4px), plus discret et cohérent avec l'identité éditoriale.
     Fond en verre dépoli (`backdrop-filter: blur`) plutôt qu'un aplat
     plein, pour rester léger visuellement tout en gardant un contraste
     garanti quelle que soit la photo du jour. */
  .article-image-overlay .eyebrow{
    display: inline-flex;
    align-items: center;
    background: rgba(16,21,28,.58);
    backdrop-filter: blur(7px) saturate(140%);
    -webkit-backdrop-filter: blur(7px) saturate(140%);
    border-left: 3px solid var(--gold);
    border-radius: 4px;
    padding: 6px 14px;
  }
  .article-image-overlay h1{
    color: var(--paper);
    max-width: 20ch;
    text-shadow: 0 3px 22px rgba(0,0,0,.6);
  }
  .article-image-overlay .pubdate{
    color: rgba(236,231,218,.78);
    margin: 0;
    text-shadow: 0 2px 12px rgba(0,0,0,.55);
  }
  @media (max-width: 720px){
    .article-image-photo-wrap{ height: min(52vh, 440px); min-height: 300px; }
    .article-image-overlay{ padding-bottom: 22px; }
    .article-image-overlay h1{ max-width: none; }
  }

  .question-box{
    background: var(--surface);
    border-left: 3px solid var(--gold);
    border-radius: 4px;
    padding: 18px 22px;
    margin: 0 0 32px;
    margin-left: -22px;
  }

  .question-label{
    display: block;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gold);
    margin: 0 0 8px;
  }

  .question-text{
    font-family: "Fraunces", serif;
    font-size: 1.2rem;
    font-weight: 500;
    line-height: 1.35;
    margin: 0;
    color: var(--paper);
  }

  /* Encart pédagogique en aparté dans le corps d'un article — même recette
     que .question-box (fond surface, filet doré à gauche, radius 4px),
     mais label "Comprendre" pour une explication de fond plutôt qu'une
     question ou un enjeu. */
  .comprendre-box{
    background: var(--surface);
    border-left: 3px solid var(--gold);
    border-radius: 4px;
    padding: 18px 22px;
    margin: 24px 0 24px;
    margin-left: -22px;
  }

  .comprendre-label{
    display: block;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gold);
    margin: 0 0 8px;
  }

  .comprendre-lead{
    font-family: "Fraunces", serif;
    font-size: 1.1rem;
    font-weight: 500;
    line-height: 1.35;
    margin: 0 0 10px;
    color: var(--paper);
  }

  .comprendre-text{
    font-size: 0.95rem;
    line-height: 1.55;
    margin: 0;
    color: var(--paper-dim);
  }

  .stakes-box{
    background: var(--surface);
    border-left: 3px solid var(--gold);
    border-radius: 4px;
    padding: 18px 22px;
    margin: 8px 0 32px;
    margin-left: -22px;
  }

  .stakes-label{
    display: block;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gold);
    margin: 0 0 8px;
  }

  .stakes-text{
    font-size: 1rem;
    line-height: 1.5;
    margin: 0;
    color: var(--paper);
  }

  .stakes-text strong{ color: var(--paper); font-weight: 600; }

  /* Ce qu'on évalue, en 3 branches étiquetées — ajouté le 2 septembre
     (retour utilisateur : « ce qu'on évalue n'est pas clair, il doit
     être plus pédagogique »). Avant ça, les 3 sous-questions
     favorable/stable/dégradé étaient enchaînées dans un seul .stakes-text
     sans repère visuel : leur ordre seul portait le lien avec les 3
     cartes plus bas, invisible pour qui ne le sait pas déjà. Remplacé
     par une liste, une ligne par branche, chacune ouverte par un mot-
     repère coloré — même code couleur que .kind-tag sur les cartes —
     pour que le rapprochement entre une branche et sa carte se fasse
     d'un coup d'œil, avant même de lire les cartes elles-mêmes. */
  .stakes-branches{
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 9px;
  }

  .stakes-branches li{
    font-size: 1rem;
    line-height: 1.5;
    color: var(--paper);
  }

  .stakes-tag{
    display: inline-block;
    font-family: "JetBrains Mono", monospace;
    font-weight: 700;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-right: 7px;
  }

  .stakes-branches li[data-kind="favorable"] .stakes-tag{ color: var(--favorable); }
  .stakes-branches li[data-kind="stable"] .stakes-tag{ color: var(--stable); }
  .stakes-branches li[data-kind="degrade"] .stakes-tag{ color: var(--degrade); }

  .essentiel-box{
    background: var(--surface);
    border: 1px solid var(--gold);
    border-radius: 10px;
    padding: 24px 26px;
    margin: 36px 0 0;
  }

  .essentiel-label{
    display: block;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gold);
    margin: 0 0 10px;
  }

  .essentiel-text{
    font-size: 1rem;
    line-height: 1.5;
    margin: 0;
    color: var(--paper);
  }

  .essentiel-text strong{ color: var(--paper); font-weight: 600; }

  .essentiel-text + .essentiel-text{ margin-top: 12px; }

  /* Delta France — indice de sens pondéré des 3 scénarios pour la France,
     dernier paragraphe de L'essentiel. Jauge en arc continu (dégradé
     rouge→or→vert, même géométrie que .gauge des cartes) avec un repère
     positionné par script à l'endroit du score (-1 à +1). Voir
     docs/ARCHITECTURE.md pour la méthode de calcul et les seuils. */
  .delta-france{
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 18px;
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid var(--hairline);
  }

  .delta-gauge{ position: relative; width: 108px; height: 78px; flex: 0 0 108px; }
  .delta-gauge svg{ width: 100%; height: 64px; overflow: visible; display: block; }
  .delta-gauge-track{ fill: none; stroke-width: 10; stroke-linecap: round; }
  .delta-gauge-marker{
    fill: var(--paper);
    stroke: var(--ink);
    stroke-width: 2;
    transition: cx 1.1s cubic-bezier(.16,.8,.3,1), cy 1.1s cubic-bezier(.16,.8,.3,1);
  }
  /* Repère dans la jauge, sur 2 lignes contenues dans la largeur de la
     boîte (108px) — jamais nowrap/débordant, voir docs/ARCHITECTURE.md
     pour l'historique des essais précédents (débordement, une seule
     ligne trop tassée...). Redondant avec .delta-word dans la phrase,
     volontairement : un repère visuel rapide sur la jauge + le mot
     développé et coloré dans le texte. */
  .delta-gauge-word{
    display: block;
    margin-top: 4px;
    text-align: center;
    line-height: 1.2;
    font-size: 0.66rem;
    color: var(--paper-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  /* flex: 1 1 220px, pas juste "1" : force le retour à la ligne sous la
     jauge dès que la colonne de texte tomberait sous ~220px, plutôt que
     de dépendre d'un seul seuil de largeur d'écran fixe (ne marchait pas
     avec un zoom de texte système, une tablette, etc. — repéré via un
     retour utilisateur avec la police système agrandie). */
  .delta-text{ margin: 0; flex: 1 1 220px; min-width: 220px; }
  .delta-text .delta-flag{ border-radius: 2px; vertical-align: 1px; margin-right: 2px; }
  /* Mot du sens ("léger négatif"...) coloré directement dans la phrase
     plutôt qu'en légende séparée sous la jauge — retour utilisateur :
     plus de doublon, un seul endroit où le mot apparaît. Couleur fixée
     par data-kind sur .delta-france (favorable/dégradé déjà utilisées
     partout ailleurs sur le site). */
  .delta-word{ color: var(--paper); }
  .delta-france[data-kind="positif"] .delta-word{ color: var(--favorable); }
  .delta-france[data-kind="negatif"] .delta-word{ color: var(--degrade); }

  @media (prefers-reduced-motion: reduce){
    .delta-gauge-marker{ transition: none; }
  }

  /* ---------- Encart liste (sorties, classements...) ---------- */
  /* Composant réutilisable pour toute liste mise en avant dans un article —
     même recette que .essentiel-box (fond surface, bordure dorée, label
     JetBrains Mono) mais pensé pour une liste d'items scannables plutôt
     qu'un paragraphe. Le repère (.list-box-rank) accepte un numéro
     (classement) ou un emoji (liste non classée) selon le contexte.
     Voir docs/ARCHITECTURE.md pour l'usage détaillé. */
  .list-box{
    background: var(--surface);
    border: 1px solid var(--gold);
    border-radius: 10px;
    padding: 22px 26px 6px;
    margin: 0 0 28px;
  }

  .list-box-label{
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gold);
    margin: 0 0 4px;
  }

  .list-box-items{
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .list-box-items li{
    display: flex;
    align-items: baseline;
    gap: 14px;
    padding: 14px 0;
    border-top: 1px dashed var(--hairline);
  }

  .list-box-rank{
    flex: none;
    font-family: "JetBrains Mono", monospace;
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--gold);
    min-width: 1.7em;
  }

  .list-box-body{
    flex: 1 1 auto;
    min-width: 0;
  }

  .list-box-title{
    display: block;
    font-size: 1rem;
    font-weight: 600;
    color: var(--paper);
  }

  .list-box-meta{
    display: block;
    font-size: 0.86rem;
    color: var(--paper-dim);
    margin-top: 3px;
    line-height: 1.45;
  }

  .list-box-foot{
    font-size: 0.86rem;
    color: var(--paper-dim);
    padding: 14px 0 16px;
    border-top: 1px dashed var(--hairline);
    margin: 0;
  }

  .indicator-strip{
    display: flex;
    gap: 28px;
    flex-wrap: wrap;
    padding-top: 24px;
    border-top: 1px dashed var(--hairline);
  }

  /* ---- Horloge de l'Apocalypse : graphique depuis 1947 (même recette
     que .comprendre-box pour l'encart, gabarit SVG inspiré de .evo-chart
     des pages de suivi, adapté à une série longue/irrégulière). ---- */
  .dc-chart-box{
    background: var(--surface);
    border-left: 3px solid var(--degrade);
    border-radius: 4px;
    padding: 20px 22px 18px;
    margin: 28px 0 0;
    margin-left: -22px;
  }

  .dc-chart-label{
    display: block;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--degrade);
    margin: 0 0 8px;
  }

  .dc-chart-lead{
    font-family: "Fraunces", serif;
    font-size: 1.05rem;
    font-weight: 500;
    line-height: 1.35;
    margin: 0 0 14px;
    color: var(--paper);
  }

  .dc-chart-box svg{ display: block; width: 100%; height: auto; overflow: visible; }
  .dc-line{ fill: none; stroke: var(--degrade); stroke-width: 2; stroke-linejoin: round; }
  .dc-dot{ fill: var(--ink); stroke: var(--degrade); stroke-width: 2; }
  .dc-dot.is-highlight{ fill: var(--degrade); stroke: var(--paper); stroke-width: 1.5; }
  .dc-gridline{ stroke: var(--hairline); stroke-width: 1; stroke-dasharray: 2 3; }
  .dc-axis{ stroke: var(--hairline); stroke-width: 1; }
  .dc-axis-label{
    font-family: "JetBrains Mono", monospace;
    font-size: 9.5px;
    fill: var(--paper-dim);
  }
  .dc-point-label{
    font-family: "JetBrains Mono", monospace;
    font-size: 10px;
    font-weight: 700;
  }
  .dc-point-label.is-favorable{ fill: var(--favorable); }
  .dc-point-label.is-degrade{ fill: var(--degrade); }

  .dc-chart-caption{
    font-size: 0.8rem;
    color: var(--paper-dim);
    margin: 12px 0 0;
    padding-top: 12px;
    border-top: 1px dashed var(--hairline);
  }

  .indicator{
    display:flex;
    flex-direction: column;
    gap: 4px;
  }

  .indicator .label{
    font-size: 0.78rem;
    color: var(--paper-dim);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  .indicator .value{
    font-family: "JetBrains Mono", monospace;
    font-size: 1.15rem;
    font-weight: 500;
  }

  .indicator .value .delta{
    color: var(--degrade);
    font-size: 0.92rem;
  }

  /* ---------- Section labels ---------- */

  .section-label{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--paper-dim);
    margin: 0 0 8px;
  }

  .section-title{
    font-family: "Fraunces", serif;
    font-size: 1.6rem;
    font-weight: 600;
    margin: 0 0 36px;
  }

  section.scenarios{ padding: 56px 0 24px; }

  /* ---------- Scenario cards ---------- */

  .cards{
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .card{
    background: var(--surface);
    border: 1px solid var(--hairline);
    /* Filet de couleur en tête de carte — ajouté le 2 septembre (retour
       utilisateur : "pas visuel"). Reprend --accent, déjà posé par
       data-kind juste plus bas, pour que favorable/stable/dégradé se
       distinguent d'un coup d'œil, avant même de lire le mot-repère.
       Même logique que le filet de .france-line (17 août). */
    border-top: 4px solid var(--accent);
    border-radius: 6px;
    padding: 26px 26px 24px;
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    gap: 30px;
  }

  .card-head{
    flex: 0 0 210px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 14px;
  }

  .card-body{
    flex: 1 1 auto;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  @media (max-width: 720px){
    .card{ flex-direction: column; gap: 18px; }
    .card-head{ flex: 0 0 auto; width: 100%; }
  }

  /* ---------- Cartes de scénarios en 3 colonnes (desktop) ---------- */
  /* Ajouté le 28 août 2026, retour utilisateur : trois cartes empilées en
     pleine largeur créent trop de scroll ("scroll fatigué"). À partir de
     860px, .cards passe en ligne et chaque .card repasse en layout
     colonne interne (même motif que le fallback mobile ci-dessus : tête
     centrée au-dessus du corps, pas à côté). .cards reste dans les 920px
     du .wrap ambiant (colonnes ~280px) pour garder la même largeur que le
     reste de la page (contexte, essentiel...) — un premier essai avait
     élargi la rangée jusqu'à 1320px (colonnes ~400px) via un breakout
     hors du .wrap, mais retour utilisateur : ça cassait l'alignement avec
     les boîtes juste en dessous (L'essentiel, la note "Ordres de
     grandeur"...), donc abandonné. L'essentiel du gain contre le scroll
     vient du passage en 3 colonnes lui-même, pas de la largeur des
     colonnes. Sous 860px, comportement strictement inchangé (cartes
     empilées comme avant). Scopé à `screen` explicitement pour ne jamais
     interférer avec `@media print` plus bas (qui a son propre layout de
     carte compact, indépendant de la largeur d'écran au moment de
     l'impression). */
  @media screen and (min-width: 860px){
    .cards{
      flex-direction: row;
      align-items: stretch;
    }
    .card{ flex: 1 1 0; flex-direction: column; gap: 16px; padding: 22px 20px 20px; }
    .card-head{ flex: 0 0 auto; width: 100%; }
    /* Les 3 titres de scénarios n'ont pas la même longueur ("La rallonge
       promise passe l'automne" passe sur 2 lignes, les deux autres sur 1) :
       sans hauteur réservée, le card-body de chaque carte démarrait à une
       hauteur différente selon la carte, cassant l'alignement de la
       rangée. On réserve systématiquement la hauteur de 2 lignes (line-
       height hérité 1.55 × 2). */
    .card-head h3{ min-height: 3.1em; display: flex; align-items: center; justify-content: center; }
  }

  .card[data-kind="favorable"]{ --accent: var(--favorable); }
  .card[data-kind="stable"]{ --accent: var(--stable); }
  .card[data-kind="degrade"]{ --accent: var(--degrade); }

  .card-top{
    display:flex;
    align-items:center;
    justify-content: space-between;
  }

  /* Style "point plein" ajouté le 14 août (retour utilisateur, option C
     validée sur mockup — code couleur qui scanne vite façon feu
     tricolore, sans pastille pleine/badge qui aurait fait trop "app
     mobile"). Mots (favorable/stable/dégradé) et palette (--favorable/
     --stable/--degrade) inchangés — habillage visuel uniquement,
     jamais appliqué rétroactivement aux archives déjà publiées. */
  .kind-tag{
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-family: "JetBrains Mono", monospace;
    font-weight: 700;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--accent);
  }
  .kind-tag::before{
    content: "";
    width: 11px;
    height: 11px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 8px var(--accent);
    flex-shrink: 0;
  }

  /* gauge — agrandie et éclaircie le 2 septembre (retour utilisateur :
     "pas visuel, trop petit"). Même géométrie qu'avant (le path SVG,
     un demi-cercle rayon 45, ne change pas : élargir la boîte suffit à
     grossir tout l'élément proportionnellement), juste un cran plus
     grande et plus épaisse, et le chiffre central passe en couleur
     --accent plutôt qu'en blanc neutre pour que le pourcentage porte
     lui-même la couleur du scénario. */
  .gauge{ position: relative; width: 132px; height: 78px; margin: 4px auto 0; }
  .gauge svg{ width: 100%; height: 100%; overflow: visible; }
  .gauge-track{ fill: none; stroke: var(--hairline); stroke-width: 12; stroke-linecap: round; }
  .gauge-value{
    fill: none;
    stroke: var(--accent);
    stroke-width: 12;
    stroke-linecap: round;
    stroke-dasharray: 141.4;
    stroke-dashoffset: 141.4;
    transition: stroke-dashoffset 1.1s cubic-bezier(.16,.8,.3,1);
  }
  .gauge-num{
    position:absolute;
    bottom: -2px;
    left: 0; right: 0;
    text-align:center;
    font-family: "JetBrains Mono", monospace;
    font-weight: 700;
    font-size: 1.7rem;
    color: var(--accent);
  }
  .gauge-word{
    text-align:center;
    font-size: 0.82rem;
    color: var(--paper-dim);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: 2px;
  }

  @media (prefers-reduced-motion: reduce){
    .gauge-value{ transition: none; }
  }

  .card h3{
    font-family: "Fraunces", serif;
    font-size: 1.28rem;
    font-weight: 600;
    margin: 0;
    text-align: center;
  }

  /* Remonté de 0.92rem à 1.05rem le 2 septembre (retour utilisateur :
     "trop petit") — ce texte est le cœur de l'article (le contenu que
     les 3 cartes existent pour porter), il n'a aucune raison d'être
     plus petit que le texte de contexte (.dek, 1.1rem) juste au-dessus
     dans la page. */
  .card .why{
    font-size: 1.05rem;
    color: var(--paper-dim);
  }

  .card .why + .why{
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px dashed var(--hairline);
  }

  .card .why strong{ color: var(--paper); font-weight: 500; }

  .divider{ border: none; border-top: 1px solid var(--hairline); margin: 0; }

  .field{ font-size: 0.88rem; }
  .field .field-label{
    display:block;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--accent);
    margin-bottom: 4px;
  }

  .field ul{
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 7px;
  }

  .field li{
    padding-left: 15px;
    position: relative;
  }

  .field li::before{
    content: "–";
    position: absolute;
    left: 0;
    color: var(--accent);
  }

  .field-name{
    display: block;
    font-size: 0.82rem;
    color: var(--paper-dim);
    margin-bottom: 2px;
  }

  .evo-current{ font-size: 1.5rem; font-weight: 700; color: var(--paper); line-height: 1; }
  .evo-arrow{ display: inline-block; font-weight: 700; font-size: 1.1rem; }
  .evo-arrow.is-up{ color: var(--favorable); }
  .evo-arrow.is-down{ color: var(--degrade); }
  .evo-arrow.is-flat{ color: var(--paper-dim); }
  .evo-prev{ font-size: 0.76rem; color: var(--paper-dim); font-weight: 400; }

  .field li .evo-current{ font-size: 1.15rem; }
  .field li .evo-arrow{ font-size: 0.95rem; }
  .field li .evo-prev{ font-size: 0.74rem; }

  .france-line{
    background: var(--surface-2);
    border-radius: 4px;
    padding: 10px 12px 10px 13px;
    /* 0.88rem → 0.95rem le 2 septembre (retour utilisateur "trop petit"),
       alignée sur .comprendre-text pour rester cohérente avec les autres
       encarts du gabarit plutôt que de rester la seule ligne de lecture
       en dessous de 0.9rem sur la page. */
    font-size: 0.95rem;
    /* Repère visuel favorable/défavorable pour la France, discret mais
       lisible au premier coup d'œil — ajouté le 17 août (retour
       utilisateur). Filet de couleur + flèche réutilisent la palette et
       les classes .evo-arrow déjà utilisées pour les indicateurs
       chiffrés (même code couleur --favorable/--degrade), plutôt que
       d'inventer un nouveau système. Couleur pilotée par
       data-france-impact, jamais par --accent (la couleur du scénario
       lui-même) : les deux jugements sont désormais indépendants, voir
       docs/routine-prompt.md. */
    border-left: 3px solid var(--paper-dim);
  }
  .france-line[data-france-impact="favorable"]{ border-left-color: var(--favorable); }
  .france-line[data-france-impact="degrade"]{ border-left-color: var(--degrade); }

  .france-line .field-label{ color: var(--accent); }

  /* ---------- Footer ---------- */

  footer{
    border-top: 1px solid var(--hairline);
    padding: 40px 0 56px;
    margin-top: 56px;
  }

  footer .caveat{
    font-size: 0.84rem;
    color: var(--paper-dim);
    margin: 0 0 24px;
  }

  .indicators-note{
    font-size: 0.78rem;
    color: var(--paper-dim);
    margin: 4px 0 0;
  }

  .indicators-note a{ color: var(--paper-dim); text-decoration: underline; text-decoration-style: dotted; }
  .indicators-note a:hover{ color: var(--paper); }

  /* Crédit de la photo de couverture, redescendu en footnote tout en bas
     de page le 29 août — retour utilisateur : juste sous le cover plein
     écran, "Photo d'illustration..." étalait le crédit trop tôt, avant
     même le premier mot de l'article ("on ne voit que ça"). JetBrains
     Mono discret pour rester de la mention légale, pas un élément à
     lire. **Déplacée le même jour à l'intérieur de `.footer-bottom`**
     (retour utilisateur : posée juste au-dessus avec son propre filet de
     séparation, elle donnait l'impression de deux footnotes empilées au
     lieu d'une seule) — désormais dans le même bloc, sous le même filet,
     que les mentions légales : une seule note en bas de page. Icône
     appareil photo ajoutée devant le texte le même jour (retour
     utilisateur, "pour faire plus joli"), toujours la même quel que soit
     le sujet du jour — décoration fixe, pas une icône par photo. */
  /* [CORRIGÉ le 30 août 2026, retour utilisateur : sur mobile, le crédit
     passait sur 2 lignes avec "Pexels" isolé tout à droite, illisible.]
     display: inline-flex sur toute la ligne empêchait le texte de
     s'enrouler normalement : le lien restait un item flex à part,
     poussé en bout de ligne au lieu de suivre le texte. Flux inline
     normal à la place (icône alignée au fil du texte via
     vertical-align) : tout s'enroule ensemble comme un paragraphe
     classique. */
  .footer-photo-credit{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.76rem;
    line-height: 1.6;
    color: var(--paper-dim);
    margin: 0;
  }
  .footer-photo-credit svg{ width: 14px; height: 14px; vertical-align: -2px; margin-right: 3px; }
  .footer-photo-credit a{ color: var(--paper-dim); text-decoration: underline; text-decoration-style: dotted; white-space: nowrap; }
  .footer-photo-credit a:hover{ color: var(--paper); }

  /* Barre du bas — mentions légales, séparée du reste du footer par un
     filet (même motif que .indicator-strip plus haut dans l'article).
     Les réseaux sociaux ne sont plus dupliqués ici : ils sont déjà
     couverts par la section .share-block#nous-suivre plus haut dans la
     page — retiré le 20 août, retour utilisateur (doublon repéré),
     voir docs/ARCHITECTURE.md. */
  .footer-bottom{
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    margin-top: 28px;
    padding-top: 24px;
    border-top: 1px solid var(--hairline);
  }

  .legal-links-label{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.76rem;
    color: var(--paper-dim);
  }

  .legal-links{
    display: flex;
    gap: 18px;
    flex-wrap: wrap;
    margin: 0;
  }

  .legal-links a{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.76rem;
    color: var(--paper-dim);
    text-decoration: none;
    border-bottom: 1px dotted transparent;
  }

  .legal-links a:hover{ color: var(--paper); border-bottom-color: var(--paper-dim); }

  /* ---------- Lexique ---------- */

  section.lexique{ padding: 8px 0 48px; }

  .cross-link{
    display: inline-block;
    margin-top: 20px;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.82rem;
    color: var(--gold);
    text-decoration: none;
    border-bottom: 1px dotted var(--gold);
    padding-bottom: 1px;
  }

  .cross-link:hover{ color: var(--paper); border-bottom-color: var(--paper); }

  .glossary{
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 14px 28px;
    margin: 0;
  }

  @media (max-width: 640px){
    .glossary{ grid-template-columns: 1fr; }
  }

  .glossary dt{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    color: var(--gold);
    margin: 0 0 3px;
  }

  .glossary dd{
    font-size: 0.88rem;
    color: var(--paper-dim);
    margin: 0;
  }

  /* ---------- Sources ---------- */

  section.sources{
    padding: 32px 0 48px;
    border-top: 1px dashed var(--hairline);
  }

  .sources-list{
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .sources-list a{
    font-size: 0.95rem;
    color: var(--paper-dim);
    text-decoration: none;
    border-bottom: 1px dotted transparent;
  }

  .sources-list a:hover{
    color: var(--paper);
    border-bottom-color: var(--paper-dim);
  }

  .btn-outline{
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    text-decoration: none;
    color: var(--gold);
    background: transparent;
    border: 1px solid var(--gold);
    border-radius: 100px;
    padding: 9px 22px;
    transition: background .15s, color .15s;
  }

  .btn-outline svg{ display: block; flex-shrink: 0; }

  .btn-outline:hover{
    background: var(--gold);
    color: var(--ink);
  }

  /* ---------- Partage ---------- */

  section.share-block{
    padding: 32px 0 48px;
    border-top: 1px dashed var(--hairline);
  }

  .share-block p{
    color: var(--paper-dim);
    margin: 0 0 20px;
  }

  .share-row{
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 8px;
  }

  .tomorrow-teaser{
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    color: var(--paper-dim);
    max-width: none;
    margin: 18px 0 0;
  }

  button.btn-outline{
    cursor: pointer;
    appearance: none;
    -webkit-appearance: none;
  }

  /* Rappel "Reste informé" compact, ajouté le 21 août juste après
     "L'essentiel", fusionné le même jour avec un lien newsletter (retour
     utilisateur : "j'aime bien ton option B" — un seul bloc à 2 actions
     plutôt que d'empiler plusieurs box séparées). Retour utilisateur
     d'origine : le seul bouton d'activation des notifications vivait
     tout en bas de page (section #notifications), après lexique/sources/
     partage, jamais vu par la plupart des lecteurs. Ce bloc-ci vise le
     moment où le lecteur vient de finir l'essentiel de l'édition, le
     plus engagé qu'il sera. Telegram volontairement absent de ce bloc
     (retour utilisateur : le mécanisme de vote ne fonctionne pas
     actuellement, sujets trop complexes pour ce format — pas de mise en
     avant supplémentaire tant que ce n'est pas réglé). Nom `.follow-*`
     réutilisé de la section "Nous suivre" déjà présente ailleurs sur le
     site (glossaire.html, contact.html...), même famille de composant. */
  .follow-inline{
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 14px 20px;
    background: var(--surface);
    border-left: 3px solid var(--gold);
    border-radius: 4px;
    padding: 16px 22px;
    margin: 20px 0 0;
  }
  .follow-inline-text{
    margin: 0;
    font-size: 0.92rem;
    color: var(--paper-dim);
  }
  .follow-inline-actions{
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }
  .follow-inline .btn-outline{ flex-shrink: 0; }


  .follow-btn{
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.74rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-decoration: none;
    color: var(--gold);
    background: transparent;
    border: 1px solid var(--gold);
    border-radius: 100px;
    padding: 8px 16px;
    flex-shrink: 0;
    transition: background .15s, color .15s;
  }
  .follow-btn:hover{ background: var(--gold); color: var(--ink); }
  .follow-btn svg{ display: block; flex-shrink: 0; }
  #nous-suivre .share-row{ gap: 8px; }

  /* Impression, ajoutée le 24 août — bouton discret dans le masthead, à
     côté de l'icône notifications (déclenche juste window.print(), voir
     le script en bas de page). Imprime l'article complet (corps de
     texte, scénarios détaillés, L'essentiel) : seuls la nav, le partage,
     l'image, le lexique, les sources et le footer sont masqués (pas
     l'objectif de la lecture papier). Le site est en thème sombre (texte
     clair sur fond sombre) : sans reprise des variables couleur, le
     texte clair resterait quasi invisible sur la page blanche imprimée
     (beaucoup de navigateurs n'impriment pas les couleurs de fond par
     défaut). Redéfinir --paper/--ink/--surface ici suffit, tout le CSS du
     site les consomme déjà via var(--paper) etc.
     Densité resserrée le 24 août (retour utilisateur : moins de pages,
     pour un rendu correct avec l'option navigateur/imprimante « 2 pages
     par feuille », qu'on ne peut pas forcer depuis une page web — c'est
     un réglage du dialogue d'impression, pas du CSS). `html{font-size}`
     rétrécit d'un coup toutes les tailles en rem (quasi tout le fichier),
     et les paddings/margins des encarts sont resserrés en plus. Chaque
     encart à bordure gauche accentuée (question-box/stakes-box/
     comprendre-box/dc-chart-box) utilise `margin-left: -22px` pour
     déborder jusqu'au bord — ne jamais réécrire leur `margin` en
     raccourci ici, ça écraserait ce -22px et casserait l'alignement :
     ajuster seulement margin-top/margin-bottom en propriétés séparées. */
  @media print{
    :root{
      --ink: #ffffff;
      --paper: #10151c;
      --paper-dim: #4a4a4a;
      --surface: #ffffff;
      --surface-2: #f0f0f0;
      --hairline: #cccccc;
    }
    html{ font-size: 82%; }
    body{ background: #fff; line-height: 1.4; }
    header.masthead, nav.topnav, .toc, .share-inline,
    .article-image, .follow-inline, section.lexique, section.sources,
    section.share-block, footer{ display: none !important; }
    .wrap{ max-width: 100%; padding: 0; }
    section.hero, section.scenarios{ padding: 0; border: none; }
    h1{ font-size: 16pt; margin: 0 0 4pt; }
    .eyebrow{ margin-bottom: 6pt; }
    .pubdate{ margin-bottom: 10pt; }
    .dek{ margin-bottom: 10px; }
    .dek:last-of-type{ margin-bottom: 14px; }
    .question-box{ padding: 10px 14px; margin-bottom: 14px; }
    .stakes-box{ padding: 10px 14px; margin-top: 0; margin-bottom: 14px; }
    .comprendre-box{ padding: 10px 14px; margin-top: 10px; margin-bottom: 10px; }
    .dc-chart-box{ padding: 10px 14px; margin-top: 14px; }
    .list-box{ padding: 14px 16px 2px; margin-bottom: 14px; }
    .list-box-items li{ padding: 8px 0; }
    .indicator-strip{ padding-top: 10px; gap: 16px; }
    .section-title{ margin-bottom: 12px; }
    .cards{ gap: 10px; }
    .card{ padding: 14px 16px; gap: 16px; }
    .card-head{ flex: 0 0 140px; gap: 8px; }
    .gauge{ width: 70px; height: 42px; margin: 2px auto 0; }
    .essentiel-box{ margin-top: 14px; padding: 14px 16px; }
    .essentiel-text + .essentiel-text{ margin-top: 8px; }
    .france-line{ padding: 8px 10px; }
    .list-box, .comprendre-box, .dc-chart-box, .card, .essentiel-box{
      break-inside: avoid;
    }
    @page{ margin: 12mm; }
  }
</style></head>
<body>
<header class="masthead">
<div class="wrap masthead-row">
<div class="brand">
<div class="brand-row">
<img alt="" class="brand-mark" src="../assets/logo.svg"/>
<div class="wordmark">Scéna<span>rio</span></div>
</div>
<div class="edition">Edition of September 12, 2026 · No. 51</div>
</div>
<div class="masthead-right">
<a aria-label="Lire en français" class="masthead-lang-btn" href="../index.html" title="Lire en français">FR</a>
<button aria-label="Activer les notifications" class="onesignal-subscribe-btn masthead-notif-btn" title="Activer les notifications" type="button"><svg aria-hidden="true" fill="none" height="16" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="16"><path d="M6 10.5a6 6 0 0 1 12 0c0 3.2 1 4.7 1.5 5.3H4.5C5 15.2 6 13.7 6 10.5Z"></path><path d="M10.3 18.5a1.8 1.8 0 0 0 3.4 0"></path></svg></button>
<button aria-label="Imprimer en 1 page" class="masthead-notif-btn" id="print-page" title="Imprimer en 1 page" type="button"><svg aria-hidden="true" fill="none" height="16" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="16"><path d="M6.5 9V4.5a1 1 0 0 1 1-1h9a1 1 0 0 1 1 1V9"></path><rect height="7" rx="1.3" width="15" x="4.5" y="9"></rect><path d="M7 14h10v5.5a1 1 0 0 1-1 1H8a1 1 0 0 1-1-1V14Z"></path></svg></button>
<span aria-hidden="true" class="masthead-divider"></span>
<a aria-label="Sujet révisé" class="masthead-notif-btn" href="../archives.html?tag=revise" title="Sujet révisé"><svg aria-hidden="true" fill="none" height="16" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="16"><path d="M4 12a8 8 0 0 1 13.7-5.7L20 8"></path><path d="M20 4v4h-4"></path><path d="M20 12a8 8 0 0 1-13.7 5.7L4 16"></path><path d="M4 20v-4h4"></path></svg></a>
<a aria-label="Récap de la semaine" class="masthead-notif-btn" href="../hebdo/2026-09-06.html" title="Récap de la semaine"><svg aria-hidden="true" fill="none" height="16" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="16"><rect height="15" rx="2" width="17" x="3.5" y="5"></rect><line x1="3.5" x2="20.5" y1="9.5" y2="9.5"></line><line x1="8" x2="8" y1="3" y2="7"></line><line x1="16" x2="16" y1="3" y2="7"></line></svg></a>
<a aria-label="Revue de presse" class="masthead-notif-btn" href="../sources.html" title="Revue de presse"><svg aria-hidden="true" fill="none" height="16" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="16"><rect height="14" rx="1.5" width="13" x="3.5" y="5.5"></rect><path d="M16.5 8.5h2.5a1.5 1.5 0 0 1 1.5 1.5v9a1.5 1.5 0 0 1-1.5 1.5h-13"></path><line x1="7" x2="13" y1="9.5" y2="9.5"></line><line x1="7" x2="13" y1="13" y2="13"></line><line x1="7" x2="11" y1="16.5" y2="16.5"></line></svg></a>
<span aria-hidden="true" class="masthead-divider"></span>
<a aria-label="Accès privé" class="masthead-notif-btn" href="../dashboard.html" title="Accès privé"><svg aria-hidden="true" fill="none" height="16" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="16"><rect height="9" rx="2" width="14" x="5" y="11"></rect><path d="M8 11V7a4 4 0 0 1 8 0v4"></path></svg></a>
</div>
</div>
</header>
<nav class="topnav">
<div class="wrap">
<a aria-current="page" href="../index.html"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="M4 11.5 12 4l8 7.5"></path><path d="M6 10v9a1 1 0 0 0 1 1h3v-6h4v6h3a1 1 0 0 0 1-1v-9"></path></svg> Accueil</a>
<a href="../archives.html"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><rect height="4" rx="1" width="16" x="4" y="4.5"></rect><path d="M5 8.5v9.5a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V8.5"></path><line x1="10" x2="14" y1="13" y2="13"></line></svg> Archives</a>
<a href="../glossaire.html"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="M12 6.5c-1.6-1.2-3.7-1.7-6-1.7v13c2.3 0 4.4.5 6 1.7 1.6-1.2 3.7-1.7 6-1.7v-13c-2.3 0-4.4.5-6 1.7Z"></path><line x1="12" x2="12" y1="6.5" y2="19.5"></line></svg> Glossaire</a>
<a href="../le-projet.html"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="m12 3 8 4.5-8 4.5-8-4.5Z"></path><path d="m4 12 8 4.5 8-4.5"></path><path d="m4 16.5 8 4.5 8-4.5"></path></svg> Le projet</a>
<a href="../newsletter.html"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><rect height="13" rx="1.5" width="17" x="3.5" y="5.5"></rect><path d="M4.5 7 12 12.5 19.5 7"></path></svg> Newsletter</a>
<a href="../contact.html"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="M4 6.5A1.5 1.5 0 0 1 5.5 5h13A1.5 1.5 0 0 1 20 6.5v8a1.5 1.5 0 0 1-1.5 1.5H10l-4 3.5V16H5.5A1.5 1.5 0 0 1 4 14.5Z"></path></svg> Contact</a>
<a href="#nous-suivre"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="M6 10.5a6 6 0 0 1 12 0c0 3.2 1 4.7 1.5 5.3H4.5C5 15.2 6 13.7 6 10.5Z"></path><path d="M10.3 18.5a1.8 1.8 0 0 0 3.4 0"></path></svg> Nous suivre</a>
<a href="https://buymeacoffee.com/scenario" rel="noopener noreferrer" style="color:var(--gold)" target="_blank"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="M5 9h11v6a4 4 0 0 1-4 4H9a4 4 0 0 1-4-4Z"></path><path d="M16 10.5h1.5a2 2 0 0 1 0 4H16"></path><path d="M8.5 4.5c-.6.7-.6 1.3 0 2M12 4.5c-.6.7-.6 1.3 0 2"></path></svg> Soutenir</a>
</div>
</nav>
<div class="weekly-banner" data-hebdo="2026-09-06" hidden="" id="weekly-banner">
<div class="wrap weekly-banner-inner">
<a class="weekly-banner-link" href="../hebdo/2026-09-06.html" id="weekly-banner-link">
<svg aria-hidden="true" class="weekly-banner-icon" fill="none" height="18" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="18"><rect height="15" rx="2" width="17" x="3.5" y="5"></rect><line x1="3.5" x2="20.5" y1="9.5" y2="9.5"></line><line x1="8" x2="8" y1="3" y2="7"></line><line x1="16" x2="16" y1="3" y2="7"></line></svg>
<span class="weekly-banner-text">Le récap de la semaine est disponible — les 7 sujets et leur scénario le plus probable.</span>
<span class="weekly-banner-cta">Lire →</span>
</a>
<button aria-label="Fermer ce message" class="weekly-banner-close" id="weekly-banner-close" type="button">
<svg aria-hidden="true" fill="none" height="15" stroke="currentColor" stroke-linecap="round" stroke-width="2" viewbox="0 0 24 24" width="15"><path d="M5 5L19 19M19 5L5 19"></path></svg>
</button>
</div>
</div>
<div class="intro-banner" hidden="" id="intro-banner">
<div class="wrap intro-banner-inner">
<button aria-label="Fermer ce message" class="intro-banner-close" id="intro-banner-close" type="button">
<svg aria-hidden="true" fill="none" height="16" stroke="currentColor" stroke-linecap="round" stroke-width="2" viewbox="0 0 24 24" width="16"><path d="M5 5L19 19M19 5L5 19"></path></svg>
</button>
<div class="intro-banner-body">
<img alt="" aria-hidden="true" class="intro-banner-icon" src="../assets/logo.svg"/>
<div>
<p class="intro-banner-lead">L'actu, oui. Et après ?</p>
<p class="intro-banner-text">Chaque jour, un sujet qui compte, décortiqué en trois scénarios chiffrés, avec une probabilité pour chacun. Jamais figée : elle évolue si la situation change.</p>
</div>
</div>
</div>
</div>
<section class="hero" id="contexte">
<figure class="article-image">
<div class="article-image-photo-wrap">
<img alt="Salle de cinéma vide, écran allumé face à des rangées de sièges rouges." class="article-image-photo" src="../assets/social/topic-images/2026-09-12-wide.jpg"/>
<div class="article-image-scrim"></div>
<div class="article-image-masthead">
<img alt="" class="article-image-logo" src="../assets/logo.svg"/>
<span class="article-image-wordmark">Scéna<span>rio</span></span>
</div>
<div class="article-image-overlay wrap">
<p class="eyebrow">Saturday, culture</p>
<h1>Hollywood: Is the Sequel Model Hitting Its Limit?</h1>
<p class="pubdate">Publié le 12 septembre 2026</p>
</div>
</div>
</figure>
<div class="wrap">
<p class="share-inline">
<span class="share-inline-label">Partager :</span>
<a aria-label="Partager sur X" href="#" id="share-x" title="Partager sur X">
<svg aria-hidden="true" fill="none" stroke="currentColor" stroke-linecap="round" stroke-width="2" viewbox="0 0 24 24"><path d="M5 5L19 19M19 5L5 19"></path></svg>
</a>
<a aria-label="Partager sur Bluesky" href="#" id="share-bluesky" title="Partager sur Bluesky">
<svg aria-hidden="true" fill="currentColor" viewbox="0 0 16 16"><path d="M3.468 1.948C5.303 3.325 7.276 6.118 8 7.616c.725-1.498 2.698-4.29 4.532-5.668C13.855.955 16 .186 16 2.632c0 .489-.28 4.105-.444 4.692-.572 2.04-2.653 2.561-4.504 2.246 3.236.551 4.06 2.375 2.281 4.2-3.376 3.464-4.852-.87-5.23-1.98-.07-.204-.103-.3-.103-.218 0-.081-.033.014-.102.218-.379 1.11-1.855 5.444-5.231 1.98-1.778-1.825-.955-3.65 2.28-4.2-1.85.315-3.932-.205-4.503-2.246C.28 6.737 0 3.12 0 2.632 0 .186 2.145.955 3.468 1.948"></path></svg>
</a>
<a aria-label="Partager sur Facebook" href="#" id="share-facebook" title="Partager sur Facebook">
<svg aria-hidden="true" fill="currentColor" viewbox="0 0 24 24"><path d="M15 4h-2a4 4 0 0 0-4 4v3H7v4h2v7h4v-7h2.6l.4-4H13V8a1 1 0 0 1 1-1h2V4z"></path></svg>
</a>
<a aria-label="Partager sur LinkedIn" href="#" id="share-linkedin" title="Partager sur LinkedIn">
<svg aria-hidden="true" fill="currentColor" viewbox="0 0 24 24"><circle cx="6" cy="6" r="2"></circle><rect height="9" width="3" x="4.5" y="10"></rect><path d="M11 10h3v1.5c.7-1 1.8-1.8 3.3-1.8 2.6 0 4.2 1.7 4.2 5.1V19h-3v-3.7c0-1.6-.6-2.6-1.9-2.6-1 0-1.6.7-1.9 1.4-.1.3-.1.6-.1 1V19h-3V10z"></path></svg>
</a>
<a aria-label="Partager sur WhatsApp" href="#" id="share-whatsapp" title="Partager sur WhatsApp">
<svg aria-hidden="true" fill="currentColor" viewbox="0 0 24 24"><path d="M12 3a9 9 0 0 0-7.8 13.4L3 21l4.8-1.2A9 9 0 1 0 12 3zm4.7 12.4c-.2.6-1.2 1.1-1.7 1.2-.4.1-1 .1-1.6-.1-.4-.1-.9-.3-1.5-.6-2.7-1.2-4.4-3.9-4.6-4.1-.1-.2-1.1-1.4-1.1-2.7 0-1.3.7-1.9.9-2.1.2-.2.5-.3.7-.3h.5c.2 0 .4 0 .6.4.2.5.7 1.7.8 1.8.1.1.1.3 0 .5-.1.2-.1.3-.3.5-.1.2-.3.4-.4.5-.1.1-.3.3-.1.6.2.3.8 1.3 1.7 2.1 1.2 1 2.1 1.4 2.5 1.5.3.1.5.1.6-.1.2-.2.7-.8.9-1.1.2-.3.4-.2.6-.1.2.1 1.5.7 1.7.8.2.1.4.2.5.3.1.2.1.7-.1 1.3z"></path></svg>
</a>
<a aria-label="Partager sur Telegram" href="#" id="share-telegram" title="Partager sur Telegram">
<svg aria-hidden="true" fill="currentColor" viewbox="0 0 24 24"><path d="M2 12l19-9-7 19-3-7-6-3z"></path></svg>
</a>
<button aria-label="Copier le lien" id="share-copy" title="Copier le lien" type="button">
<svg aria-hidden="true" class="share-icon-link" fill="none" stroke="currentColor" stroke-linecap="round" stroke-width="2" viewbox="0 0 24 24"><path d="M10 14a5 5 0 0 0 7.5.5l2-2a5 5 0 0 0-7-7l-1 1"></path><path d="M14 10a5 5 0 0 0-7.5-.5l-2 2a5 5 0 0 0 7 7l1-1"></path></svg>
<svg aria-hidden="true" class="share-icon-check" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" viewbox="0 0 24 24"><path d="M5 13l4 4L19 7"></path></svg>
</button>
</p>
<nav aria-label="Sommaire de l'édition" class="toc">
<a href="#scenarios">Scénarios</a>
<a href="#essentiel">L'essentiel</a>
<a href="#lexique">Référence</a>
</nav>
<div class="question-box">
<span class="question-label">La question posée</span>
<p class="question-text">In 2026, several big-budget sequels flopped in theaters while original films thrived: is the franchise bet still as safe as before?</p>
</div>
<p class="section-label">Les faits</p>
<p class="dek">A single film, <strong>Spider-Man: Brand New Day</strong> — the sequel to the Spider-Man/Avengers showdown we already covered (<a href="../archives/2026-07-18.html">read our article</a>) — grossed <strong>$2.2 billion</strong> worldwide in 2026 and already holds the record for the biggest launch in history ($360 million in a single weekend in the US). At the same time, three other mega-budget sequels — Star Wars: The Mandalorian and Grogu, Masters of the Universe, and Supergirl — failed at the box office, each costing over $150 million without breaking even. Even Avatar: Fire and Ash, from the most profitable franchise<a aria-label="See definition in glossary" class="lex-ref" href="#lex-franchise">*</a> in cinema history, only grossed about $1.45 billion worldwide — a third less than the previous film in the saga, released three years earlier. The system of sequels, spin-offs<a aria-label="See definition in glossary" class="lex-ref" href="#lex-spin-off">*</a>, and reboots<a aria-label="See definition in glossary" class="lex-ref" href="#lex-reboot">*</a> that has driven Hollywood for twenty years is showing its first visible cracks this year.</p>
<p class="dek">Since 2010, sequels, remakes, and prequels have occupied eight to ten out of ten spots in the annual ranking of the biggest US box office hits — nine out of ten still in 2024. In 2026, that number dropped to <strong>five out of ten</strong>: two entirely original, ultra-low-budget films, Obsession (less than $1 million invested) and Backrooms, each surpassed $300 million in worldwide revenue, driven by word-of-mouth rather than a big studio marketing campaign. A musical biopic, Michael, and a sci-fi novel adaptation, Project Hail Mary, round out this unusual podium of films without sequels or extended universes.</p>
<div class="comprendre-box">
<span class="comprendre-label">Comprendre</span>
<p class="comprendre-lead">Un film de franchise reste, en moyenne, bien plus rentable qu'un film inédit — ce qui explique pourquoi les studios continuent d'en produire autant malgré les échecs les plus visibles.</p>
<p class="comprendre-text">Un film de franchise qui réussit rapporte en moyenne 3,1 fois sa mise (retour sur investissement<a aria-label="Voir la définition dans le lexique" class="lex-ref" href="#lex-roi">*</a>, ou ROI), contre 1,8 fois pour un film inédit qui réussit. Mais cette moyenne cache une réalité inégale. La plupart des films inédits ne rapportent jamais leur budget. Une franchise connue, elle, attire un public de base même quand le film déçoit. Les studios misent donc sur les franchises comme un pari moins risqué en moyenne, pas comme une garantie individuelle de succès.</p>
</div>
<p class="dek">The three biggest US studios are not reacting the same way to these signals. Universal continues to line up sequels and horror films, its annual CinemaCon<a aria-label="See definition in glossary" class="lex-ref" href="#lex-cinemacon">*</a> presentation to exhibitors remaining entirely focused on these types of films. Disney has locked in <strong>eleven sequels and live-action remakes through 2029</strong>, including Avengers: Secret Wars and Frozen 3, without slowing down despite the year's failures. Warner Bros., weakened a year earlier by its mega-$110 billion merger with Discovery that we detailed (<a href="../archives/2026-07-26.html">read our article</a>), conversely touts an "eclectic catalog": its co-president Pamela Abdy claims to bet on both franchises and original big-budget works — a gamble analysts considered risky a year ago, before Sinners and One Battle After Another, two original films produced by the same studio, swept six Oscars in 2026, including Best Picture.</p>
<p class="dek">Here, studio by studio, is how each of the three largest US players is reacting to the same warning signs:</p>
<div class="list-box">
<span class="list-box-label">Trois studios, trois stratégies face aux mêmes échecs</span>
<ul class="list-box-items">
<li>
<span class="list-box-rank">1</span>
<span class="list-box-body">
<span class="list-box-title">Disney — tout misé sur les franchises</span>
<span class="list-box-meta">Onze suites et remakes verrouillés jusqu'en 2029 (dont Avengers : Secret Wars, Frozen 3) · aucun changement de cap annoncé après les échecs de 2026.</span>
</span>
</li>
<li>
<span class="list-box-rank">2</span>
<span class="list-box-body">
<span class="list-box-title">Universal — cap inchangé sur suites et horreur</span>
<span class="list-box-meta">Présentation CinemaCon 2026 entièrement tournée vers les franchises et les films d'horreur, sans inflexion visible.</span>
</span>
</li>
<li>
<span class="list-box-rank">3</span>
<span class="list-box-body">
<span class="list-box-title">Warner Bros. — catalogue éclectique assumé</span>
<span class="list-box-meta">Sorties théâtrales en hausse (11 en 2025, jusqu'à 18 prévues en 2027) · mêle franchises et œuvres originales, récompensées aux Oscars 2026.</span>
</span>
</li>
</ul>
<p class="list-box-foot">Deux studios sur trois n'ont, à ce stade, changé aucune stratégie malgré les échecs de 2026 — le pari de Warner Bros. reste, pour l'instant, une exception plutôt qu'une nouvelle norme du secteur.</p>
</div>
<p class="dek">The problem isn't that Universal and Disney are ignoring the warning signs: it's that they are betting on their ability to absorb individual failures through volume and a few very big hits, rather than changing their approach.</p>
<div class="comprendre-box">
<span class="comprendre-label">Comprendre</span>
<p class="comprendre-lead">« Fatigue des franchises » ne veut peut-être pas dire ce qu'on croit : le problème ressemble autant à un embouteillage de sorties qu'à un rejet du principe même des suites.</p>
<p class="comprendre-text">Masters of the Universe a obtenu de bons avis (86 % d'avis positifs du public) mais n'a rapporté que 110 millions de dollars pour un budget d'environ 180 millions. Il est sorti à quelques semaines de plusieurs autres grosses sorties, dont deux succès surprises inattendus. Un public qui ne peut pas tout voir choisit alors le film qui lui semble le plus neuf, pas nécessairement celui qui a le meilleur accueil critique.</p>
</div>
<p class="dek">The weakness is not limited to the US audience, either. We already covered how Hollywood is losing ground to local cinema in China, long its second-largest global market (<a href="archives/2026-08-22.html">read our article</a>): major American franchises are also declining there, replaced by successful Chinese productions. A model designed to dominate a homogeneous global market is therefore running out of steam on several fronts at once, not just in front of a US audience tired of sequels.</p>
<p class="dek">For French cinema, this Hollywood stumble has an unexpected effect. Over the whole of 2025, French films captured <strong>37.9% of the box office</strong>, compared to 34.9% for American films — their lowest level since 1982. In the first five months of 2026, the gap narrowed further (44.4% vs. 45%). The French system, where the CNC<a aria-label="See definition in glossary" class="lex-ref" href="#lex-cnc">*</a> funds part of national cinema via the advance on receipts<a aria-label="See definition in glossary" class="lex-ref" href="#lex-avance-sur-recettes">*</a> independently of expected box office success alone, leaves room for original films that the purely commercial Hollywood system increasingly reserves. France has its own franchises (Asterix, Kaamelott), but they weigh far less on its box office than American franchises do on theirs.</p>
<div class="indicator-strip">
<div class="indicator">
<span class="label">Share of sequels/franchises in the North American box office top 10</span>
<span class="value">50% <span class="delta">(vs. 90% in 2024)</span></span>
</div>
<div class="indicator">
<span class="label">Sequels with budgets over $150M that miss their mark in theaters</span>
<span class="value">At least 3 <span class="delta">(in 2026, as in 2025)</span></span>
</div>
</div>
</div>
</section>
<section class="scenarios" id="scenarios">
<div class="wrap">
<p class="section-label">Favorable, stable ou dégradé</p>
<h2 class="section-title">Hollywood peut-il encore compter sur ses franchises ?</h2>
<div class="stakes-box">
<span class="stakes-label">Ce qu'on évalue</span>
<ul class="stakes-branches">
<li data-kind="favorable"><span class="stakes-tag">Favorable</span>Le cinéma se rééquilibre : les films inédits confirment leur place aux côtés de franchises mieux calibrées, sans crise financière.</li>
<li data-kind="stable"><span class="stakes-tag">Stable</span>Les studios maintiennent leur rythme de suites malgré les échecs, compensés par quelques gros succès, sans changer de stratégie.</li>
<li data-kind="degrade"><span class="stakes-tag">Dégradé</span>Les suites coûteuses continuent d'échouer en série, forçant des annulations et une vraie contraction de la production.</li>
</ul>
</div>
<div class="cards">
<article class="card" data-kind="favorable">
<div class="card-head">
<span class="kind-tag">Favorable</span>
<div class="gauge">
<svg viewbox="0 0 108 64">
<path class="gauge-track" d="M9,58 A45,45 0 0,1 99,58"></path>
<path class="gauge-value" d="M9,58 A45,45 0 0,1 99,58" data-pct="27"></path>
</svg>
<div class="gauge-num">27%</div>
</div>
<div class="gauge-word">Likely</div>
<h3>Original films gain lasting ground</h3>
</div>
<div class="card-body">
<p class="why">The rebalancing that began in 2026 continues: studios are investing more in original films with controlled budgets, following the winning bet of Warner Bros. with <strong>Sinners</strong> and <strong>One Battle After Another</strong>, rewarded at the Oscars the same year three big-budget sequels flopped. The major franchises that are still released are better calibrated — tighter budgets, fewer competing releases in the same month — and find their audience without multiplying costly failures like in 2025 and 2026.</p>
<p class="why"><strong>This is the least likely of the three scenarios</strong>, because it assumes that Disney and Universal, which have shown no sign of changing strategy so far, also start reducing their production of sequels — not just Warner Bros. Less likely than the stable one, which requires no change of course. Also less likely than the degraded one, because the pace of mega-budget releases scheduled through 2029 (Frozen 3, Avengers: Secret Wars) shows no sign of slowing down at this stage.</p>
<hr class="divider"/>
<div class="field">
<span class="field-label">Indicateurs touchés</span>
<ul>
<li>
<span class="field-name">Part des suites/franchises dans le top 10 US</span>
<span class="evo-current">35-45 % (le rééquilibrage se poursuit)</span> <span class="evo-arrow is-down">↓</span> <span class="evo-prev">(vs 50 % en 2026)</span>
</li>
<li>
<span class="field-name">Suites à 150 M$+ qui ratent leur budget</span>
<span class="evo-current">1-2 en 2027 (net recul)</span> <span class="evo-arrow is-down">↓</span> <span class="evo-prev">(vs au moins 3 en 2026)</span>
</li>
</ul>
</div>
<div class="france-line" data-france-impact="favorable"><span class="field-label">The France angle</span>Since 2025, French films have captured an unprecedented market share in theaters for over forty years (37.9% vs. 34.9% for American films over the whole year); a Hollywood that reinvents itself without collapsing reinforces this dynamic without lowering overall attendance, on which theaters showing French films also depend. <span class="evo-arrow is-up">↑</span> Rather favorable for France.</div>
</div>
</article>
<article class="card" data-kind="stable">
<div class="card-head">
<span class="kind-tag">Stable</span>
<div class="gauge">
<svg viewbox="0 0 108 64">
<path class="gauge-track" d="M9,58 A45,45 0 0,1 99,58"></path>
<path class="gauge-value" d="M9,58 A45,45 0 0,1 99,58" data-pct="48"></path>
</svg>
<div class="gauge-num">48%</div>
</div>
<div class="gauge-word">Likely</div>
<h3>Studios continue to bet on sequels despite failures</h3>
</div>
<div class="card-body">
<p class="why">Disney keeps its <strong>eleven sequels and remakes locked in through 2029</strong>, Universal focuses its annual exhibitor presentation on franchises and horror, and the 2026 failures are absorbed as a blip rather than a wake-up call. The big expected hits — Avengers: Doomsday in December 2026, a whole year carried by Spider-Man: Brand New Day — are enough to maintain the sector's average profitability, even if some individual films disappoint.</p>
<p class="why"><strong>This is the most likely of the three scenarios</strong>, because it exactly extends the current situation: two out of three studios have announced no change in strategy, despite repeated failures. More likely than the favorable one, which would require a structural shift that nothing currently indicates at Disney or Universal. Also more likely than the degraded one, because total box office is still growing in 2026 ($9.8 billion expected, vs. $8.6 billion in 2025), which limits the immediate financial pressure on studios.</p>
<hr class="divider"/>
<div class="field">
<span class="field-label">Indicateurs touchés</span>
<ul>
<li>
<span class="field-name">Part des suites/franchises dans le top 10 US</span>
<span class="evo-current">45-55 % (niveau de 2026 maintenu)</span> <span class="evo-arrow is-flat">→</span> <span class="evo-prev">(vs 50 % en 2026)</span>
</li>
<li>
<span class="field-name">Suites à 150 M$+ qui ratent leur budget</span>
<span class="evo-current">environ 3 en 2027 (rythme inchangé)</span> <span class="evo-arrow is-flat">→</span> <span class="evo-prev">(vs au moins 3 en 2026)</span>
</li>
</ul>
</div>
<div class="france-line" data-france-impact="favorable"><span class="field-label">The France angle</span>The French film financing system (CNC advance on receipts, independent of expected box office success alone) partially protects French films from a mandatory sequel logic; their market share therefore remains historically strong (44.4% over the first five months of 2026, vs. 45% for American films). <span class="evo-arrow is-up">↑</span> Rather favorable for France.</div>
</div>
</article>
<article class="card" data-kind="degrade">
<div class="card-head">
<span class="kind-tag">Degraded</span>
<div class="gauge">
<svg viewbox="0 0 108 64">
<path class="gauge-track" d="M9,58 A45,45 0 0,1 99,58"></path>
<path class="gauge-value" d="M9,58 A45,45 0 0,1 99,58" data-pct="25"></path>
</svg>
<div class="gauge-num">25%</div>
</div>
<div class="gauge-word">Unlikely</div>
<h3>Big-budget sequel failures multiply, studios cut back</h3>
</div>
<div class="card-body">
<p class="why">After Star Wars: The Mandalorian and Grogu, Masters of the Universe, and Supergirl in 2026, new sequels with budgets over $150 million fail to break even in 2027. Even Avatar, the most profitable franchise in history, has already lost a third of its audience between its second and third installments. The studios, caught off guard, cancel or postpone already announced projects and reduce their investments in original films rather than franchises — the opposite of Warner Bros.' gamble.</p>
<p class="why"><strong>This scenario remains less likely than the stable one</strong>, because no studio has yet announced budget cuts or project cancellations in response to the 2026 failures. But it is more likely than the favorable one, because the number of big-budget sequels that miss their mark has remained high for two consecutive years (three in 2025, three in 2026), without any studio other than Warner Bros. having changed strategy to reverse it.</p>
<hr class="divider"/>
<div class="field">
<span class="field-label">Indicateurs touchés</span>
<ul>
<li>
<span class="field-name">Part des suites/franchises dans le top 10 US</span>
<span class="evo-current">70-80 % (retour en force malgré les échecs)</span> <span class="evo-arrow is-up">↑</span> <span class="evo-prev">(vs 50 % en 2026)</span>
</li>
<li>
<span class="field-name">Suites à 150 M$+ qui ratent leur budget</span>
<span class="evo-current">5 ou plus en 2027 (accélération)</span> <span class="evo-arrow is-up">↑</span> <span class="evo-prev">(vs au moins 3 en 2026)</span>
</li>
</ul>
</div>
<div class="france-line" data-france-impact="degrade"><span class="field-label">The France angle</span>A contraction in the number of major US releases would reduce overall attendance at French theaters, on which French films also depend to fill cinemas between two national releases — a decline in total admissions would weigh on the entire exhibition sector, including French films. <span class="evo-arrow is-down">↓</span> Rather unfavorable for France.</div>
</div>
</article>
</div>
<p class="indicators-note">Ordres de grandeur indicatifs pour les 3 scénarios ci-dessus, estimés avec l'information disponible à la publication et réévalués si la situation change — jamais des prévisions garanties. <a href="../le-projet.html">En savoir plus sur notre méthode →</a></p>
<div class="essentiel-box" id="essentiel">
<span class="essentiel-label">L'essentiel</span>
<p class="essentiel-text">Après plusieurs suites à gros budget qui ont flanché en salle en 2026, le modèle des franchises qui fait tourner Hollywood depuis vingt ans reste-t-il aussi rentable qu'avant ?</p>
<p class="essentiel-text">En 2026, la part des suites et remakes dans le top 10 du box-office nord-américain est tombée à 50 %, contre 90 % en 2024, pendant qu'au moins trois franchises à plus de 150 millions de dollars de budget (Star Wars, Masters of the Universe, Supergirl) ratent leur pari en salle.</p>
<p class="essentiel-text">Le plus probable <strong>(48 %)</strong> : les studios maintiennent leur rythme de suites malgré les échecs, portés par les gros succès attendus comme Avengers : Doomsday, sans changer de stratégie ni traverser de vraie crise financière.</p>
<p class="essentiel-text">Signal à surveiller : le résultat au box-office d'Avengers : Doomsday à sa sortie le 18 décembre 2026, et l'annonce ou non de nouvelles coupes budgétaires chez Disney ou Universal lors de la publication de leurs résultats trimestriels début 2027.</p>
<div class="delta-france" data-kind="positif">
<div class="delta-gauge">
<svg viewbox="0 0 108 64">
<defs>
<lineargradient id="deltaGrad" x1="0%" x2="100%" y1="0%" y2="0%">
<stop offset="0%" style="stop-color:var(--degrade)"></stop>
<stop offset="50%" style="stop-color:var(--stable)"></stop>
<stop offset="100%" style="stop-color:var(--favorable)"></stop>
</lineargradient>
</defs>
<path class="delta-gauge-track" d="M9,58 A45,45 0 0,1 99,58" stroke="url(#deltaGrad)"></path>
<circle class="delta-gauge-marker" cx="54" cy="13" data-score="0.50" r="5"></circle>
</svg>
<div class="delta-gauge-word">Assez positif</div>
</div>
<p class="essentiel-text delta-text"><svg aria-hidden="true" class="delta-flag" height="11" viewbox="0 0 21 15" width="16"><rect fill="#2a4d8f" height="15" width="7" x="0" y="0"></rect><rect fill="#ece7da" height="15" width="7" x="7" y="0"></rect><rect fill="#bd6248" height="15" width="7" x="14" y="0"></rect></svg> <strong>Notre évaluation de l'impact pour la France : <span class="delta-word">assez positif</span>.</strong> Les scénarios favorable (27 %) et stable (48 %), soit 75 % de probabilité cumulée, profitent tous deux à la part de marché historiquement forte des films français en salle ; seul le dégradé (25 %), en réduisant le nombre total de sorties américaines et donc la fréquentation globale, pèserait sur l'ensemble de l'exploitation française.</p>
</div>
</div>
<div class="follow-inline">
<p class="follow-inline-text">Ne rate pas la prochaine édition :</p>
<div class="follow-inline-actions">
<button class="onesignal-subscribe-btn btn-outline" type="button"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="M6 10.5a6 6 0 0 1 12 0c0 3.2 1 4.7 1.5 5.3H4.5C5 15.2 6 13.7 6 10.5Z"></path><path d="M10.3 18.5a1.8 1.8 0 0 0 3.4 0"></path></svg> <span class="btn-label">Activer les notifications</span></button>
<a class="btn-outline" href="../newsletter.html"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><rect height="13" rx="1.5" width="17" x="3.5" y="5.5"></rect><path d="M4.5 7 12 12.5 19.5 7"></path></svg> Newsletter</a>
</div>
</div>
</div>
</section>
<section class="lexique" id="lexique">
<div class="wrap">
<p class="section-label">Pour ceux qui découvrent le sujet</p>
<h2 class="section-title">Petit lexique</h2>
<dl class="glossary">
<dt id="lex-franchise">Franchise (cinematic)</dt>
<dd>A series of films sharing the same characters or universe, designed to produce multiple sequels, spin-offs, or remakes over several years.</dd>
<dt id="lex-spin-off">Spin-off</dt>
<dd>A film derived from an existing franchise, centered on a secondary character or a part of the original universe rather than its main storyline.</dd>
<dt id="lex-reboot">Reboot</dt>
<dd>A fresh start for a franchise, starting from scratch (new actors, new continuity) rather than continuing the story of previous films.</dd>
<dt id="lex-roi">Return on Investment (ROI)</dt>
<dd>The ratio between the money a film earns and its production cost — an ROI of 3 means the film earned three times its investment.</dd>
<dt id="lex-cinemacon">CinemaCon</dt>
<dd>The annual gathering of US movie theater exhibitors, where studios present their upcoming films to convince cinemas to book screens.</dd>
<dt id="lex-cnc">CNC</dt>
<dd>The National Center for Cinema, the French public body that funds part of national cinema independently of expected box office success alone.</dd>
<dt id="lex-avance-sur-recettes">Advance on Receipts</dt>
<dd>A financial grant from the CNC awarded to a film before its release, to be repaid only if it finds its audience — a safety net that allows funding films without a guarantee of commercial success.</dd>
</dl>
<a class="cross-link" href="../glossaire.html">Voir tous les termes déjà expliqués → Glossaire</a>
</div>
</section>
<section class="sources" id="sources">
<div class="wrap">
<p class="section-label">Pour aller plus loin</p>
<h2 class="section-title">Sources</h2>
<ul class="sources-list">
<li><a href="https://variety.com/2026/film/box-office/box-office-hits-flops-michael-supergirl-1236809979/" rel="noopener noreferrer" target="_blank">Variety — Box-office 2026 : succès et échecs, Michael, Supergirl et plus ↗</a></li>
<li><a href="https://screenrant.com/real-reason-movie-franchises-2026-box-office-bombs/" rel="noopener noreferrer" target="_blank">ScreenRant — La vraie raison des échecs des franchises au box-office 2026 ↗</a></li>
<li><a href="https://www.hollywoodreporter.com/movies/movie-news/warner-bros-co-ceo-pamela-abdy-t-1236697741/" rel="noopener noreferrer" target="_blank">The Hollywood Reporter — Warner Bros. persiste dans son « catalogue éclectique » ↗</a></li>
<li><a href="https://www.the-numbers.com/box-office-records/domestic/all-movies/cumulative/released-in-2026" rel="noopener noreferrer" target="_blank">The Numbers — Classement du box-office nord-américain 2026 ↗</a></li>
</ul>
<p style="margin-top:16px"><a href="../sources.html#2026-09-12" style="color:var(--gold);text-decoration:none">Voir aussi la revue de presse du jour →</a></p>
</div>
</section>
<section class="share-block" id="nous-suivre">
<div class="wrap">
<p class="section-label">Reste connecté</p>
<h2 class="section-title">Vote avant le résultat, retrouve-nous partout</h2>
<p>Chaque jour, un sondage sur notre canal Telegram : vote pour le scénario que tu juges le plus probable avant même de découvrir les vraies probabilités ci-dessus.</p>
<p>Retrouve-nous aussi sur tous nos réseaux :</p>
<div class="share-row">
<a class="follow-btn" href="https://x.com/scenario_fr" rel="noopener noreferrer" target="_blank"><svg aria-hidden="true" fill="none" height="13" stroke="currentColor" stroke-linecap="round" stroke-width="2.2" viewbox="0 0 24 24" width="13"><line x1="4" x2="20" y1="4" y2="20"></line><line x1="20" x2="4" y1="4" y2="20"></line></svg> X</a>
<a class="follow-btn" href="https://bsky.app/profile/scenario-actu.bsky.social" rel="noopener noreferrer" target="_blank"><svg aria-hidden="true" fill="currentColor" height="14" viewbox="0 0 16 16" width="14"><path d="M3.468 1.948C5.303 3.325 7.276 6.118 8 7.616c.725-1.498 2.698-4.29 4.532-5.668C13.855.955 16 .186 16 2.632c0 .489-.28 4.105-.444 4.692-.572 2.04-2.653 2.561-4.504 2.246 3.236.551 4.06 2.375 2.281 4.2-3.376 3.464-4.852-.87-5.23-1.98-.07-.204-.103-.3-.103-.218 0-.081-.033.014-.102.218-.379 1.11-1.855 5.444-5.231 1.98-1.778-1.825-.955-3.65 2.28-4.2-1.85.315-3.932-.205-4.503-2.246C.28 6.737 0 3.12 0 2.632 0 .186 2.145.955 3.468 1.948"></path></svg> Bluesky</a>
<a class="follow-btn" href="https://www.linkedin.com/company/136694258/" rel="noopener noreferrer" target="_blank"><svg aria-hidden="true" height="14" viewbox="0 0 24 24" width="14"><rect fill="none" height="19" rx="4" stroke="currentColor" stroke-width="1.6" width="19" x="2.5" y="2.5"></rect><circle cx="7.7" cy="7.6" fill="currentColor" r="1.3"></circle><line stroke="currentColor" stroke-linecap="round" stroke-width="1.8" x1="7.7" x2="7.7" y1="10.6" y2="17.3"></line><path d="M11.6 17.3v-4.2c0-1.5 1-2.4 2.3-2.4s2.3.9 2.3 2.4v4.2M11.6 10.6v.1" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8"></path></svg> LinkedIn</a>
<a class="follow-btn" href="https://www.facebook.com/share/1DZVhe3KtR/" rel="noopener noreferrer" target="_blank"><svg aria-hidden="true" height="15" viewbox="0 0 24 24" width="15"><rect fill="#000" height="22" rx="6" stroke="currentColor" stroke-width="1" width="22" x="1" y="1"></rect><rect fill="#fff" height="9" width="2.4" x="10.3" y="9.5"></rect><rect fill="#fff" height="2.6" rx="1" width="4.7" x="10.3" y="6"></rect><rect fill="#fff" height="2" width="6.5" x="7.8" y="11.6"></rect></svg> Facebook</a>
<a class="follow-btn" href="https://www.instagram.com/scenarios.actu/" rel="noopener noreferrer" target="_blank"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" viewbox="0 0 24 24" width="14"><rect height="20" rx="5" ry="5" width="20" x="2" y="2"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" x2="17.51" y1="6.5" y2="6.5"></line></svg> Instagram</a>
<a class="follow-btn" href="https://t.me/scenario_fr" rel="noopener noreferrer" target="_blank"><svg aria-hidden="true" fill="currentColor" height="14" viewbox="0 0 24 24" width="14"><path d="M2 12l19-9-7 19-3-7-6-3z"></path></svg> Telegram</a>
</div>
<div class="share-row" style="margin-top:14px">
<button class="onesignal-subscribe-btn btn-outline" id="onesignal-subscribe-btn" type="button"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="M6 10.5a6 6 0 0 1 12 0c0 3.2 1 4.7 1.5 5.3H4.5C5 15.2 6 13.7 6 10.5Z"></path><path d="M10.3 18.5a1.8 1.8 0 0 0 3.4 0"></path></svg> <span class="btn-label">Activer les notifications</span></button>
<a class="btn-outline" href="https://buymeacoffee.com/scenario" rel="noopener noreferrer" target="_blank">☕ Nous offrir un café</a>
</div>
</div>
</section>
<footer>
<div class="wrap">
<div class="footer-bottom">
<p class="footer-photo-credit"><svg aria-hidden="true" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.7" viewbox="0 0 24 24" width="14"><path d="M4 8.5a1.5 1.5 0 0 1 1.5-1.5h2l1-1.5h7l1 1.5h2A1.5 1.5 0 0 1 20 8.5v9a1.5 1.5 0 0 1-1.5 1.5h-13A1.5 1.5 0 0 1 4 17.5Z"></path><circle cx="12" cy="12.5" r="3.2"></circle></svg> Photo d'illustration. Kamil / <a href="https://www.pexels.com/photo/interior-design-of-cinema-18758034/" rel="noopener noreferrer" target="_blank">Pexels ↗</a></p>
<div class="legal-links">
<span class="legal-links-label">Envie d'en savoir plus sur nos règles ?</span>
<a href="../mentions-legales.html">Mentions légales</a>
<a href="../politique-de-confidentialite.html">Politique de confidentialité</a>
</div>
</div>
</div>
</footer>
<script>
  document.querySelectorAll('.gauge-value').forEach(function(el){
    var pct = parseFloat(el.dataset.pct) / 100;
    var offset = 141.4 * (1 - pct);
    requestAnimationFrame(function(){
      setTimeout(function(){ el.style.strokeDashoffset = offset; }, 120);
    });
  });

  document.querySelectorAll('.delta-gauge-marker').forEach(function(el){
    var score = parseFloat(el.dataset.score);
    var angleRad = (90 - score * 90) * Math.PI / 180;
    var cx = 54 + 45 * Math.cos(angleRad);
    var cy = 58 - 45 * Math.sin(angleRad);
    requestAnimationFrame(function(){
      setTimeout(function(){ el.setAttribute('cx', cx); el.setAttribute('cy', cy); }, 120);
    });
  });

</script>
<script>
  (function(){
    var ed = document.querySelector(".edition");
    var pd = document.querySelector(".pubdate");
    var datePart = "";
    if (ed && pd) {
      var m = ed.textContent.match(/Édition du\s+(.+?)\s+·/);
      if (m) { datePart = "Publié le " + m[1]; }
    }

    var contentEls = document.querySelectorAll(".dek, .why, dd");
    var text = "";
    contentEls.forEach(function(el){ text += " " + el.textContent; });
    var words = text.trim().split(/\s+/).filter(Boolean).length;
    var minutes = words ? Math.max(1, Math.round(words / 200)) : 0;
    var readingPart = minutes ? "~" + minutes + " min de lecture" : "";

    if (pd) {
      pd.textContent = [datePart, readingPart].filter(Boolean).join(" · ");
    }

    if (pd) {
      var rcCanon = document.querySelector('link[rel="canonical"]');
      var rcPath = rcCanon
        ? rcCanon.getAttribute("href").replace(/^https?:\/\/[^/]+\//, "")
        : location.pathname.replace(/^\//, "");
      fetch("https://scenario.goatcounter.com/counter/" + rcPath + ".json")
        .then(function(r){ return r.ok ? r.json() : null; })
        .then(function(d){
          var n = d && parseInt(d.count, 10);
          if (n) {
            var base = pd.textContent || "";
            pd.textContent = base ? base + " · Lu " + n.toLocaleString("fr-FR") + " fois" : "Lu " + n.toLocaleString("fr-FR") + " fois";
          }
        })
        .catch(function(){});
    }
  })();
</script>
<script>
  (function(){
    var pageUrl = window.location.href;
    if (window.location.pathname.indexOf("/archives/") === -1) {
      var parisNow = new Date(new Date().toLocaleString("en-US", { timeZone: "Europe/Paris" }));
      var yyyy = parisNow.getFullYear();
      var mm = String(parisNow.getMonth() + 1).padStart(2, "0");
      var dd = String(parisNow.getDate()).padStart(2, "0");
      pageUrl = "https://lesscenarios.fr/archives/" + yyyy + "-" + mm + "-" + dd + ".html";
    }
    var titleEl = document.querySelector("h1");
    var pageTitle = titleEl ? titleEl.textContent.trim() : document.title;
    var encodedUrl = encodeURIComponent(pageUrl);
    var encodedTitle = encodeURIComponent(pageTitle);

    var xBtn = document.getElementById("share-x");
    if (xBtn) xBtn.href = "https://twitter.com/intent/tweet?url=" + encodedUrl + "&text=" + encodedTitle;

    var bskyBtn = document.getElementById("share-bluesky");
    if (bskyBtn) bskyBtn.href = "https://bsky.app/intent/compose?text=" + encodeURIComponent(pageTitle + " " + pageUrl);

    var fbBtn = document.getElementById("share-facebook");
    if (fbBtn) fbBtn.href = "https://www.facebook.com/sharer/sharer.php?u=" + encodedUrl;

    var liBtn = document.getElementById("share-linkedin");
    if (liBtn) liBtn.href = "https://www.linkedin.com/sharing/share-offsite/?url=" + encodedUrl;

    var waBtn = document.getElementById("share-whatsapp");
    if (waBtn) waBtn.href = "https://api.whatsapp.com/send?text=" + encodedTitle + "%20" + encodedUrl;

    var tgBtn = document.getElementById("share-telegram");
    if (tgBtn) tgBtn.href = "https://t.me/share/url?url=" + encodedUrl + "&text=" + encodedTitle;

    var copyBtn = document.getElementById("share-copy");
    if (copyBtn){
      copyBtn.addEventListener("click", function(){
        if (navigator.clipboard && navigator.clipboard.writeText){
          navigator.clipboard.writeText(pageUrl).then(function(){
            copyBtn.classList.add("is-copied");
            copyBtn.setAttribute("aria-label", "Lien copié");
            copyBtn.setAttribute("title", "Lien copié");
            setTimeout(function(){
              copyBtn.classList.remove("is-copied");
              copyBtn.setAttribute("aria-label", "Copier le lien");
              copyBtn.setAttribute("title", "Copier le lien");
            }, 2000);
          });
        }
      });
    }

    var printBtn = document.getElementById("print-page");
    if (printBtn) printBtn.addEventListener("click", function(){ window.print(); });
  })();
</script>
<script async="" data-goatcounter="https://scenario.goatcounter.com/count" src="//gc.zgo.at/count.js"></script>
<script defer="" src="../assets/pwa-install.js"></script>
<script defer="" src="https://cdn.onesignal.com/sdks/web/v16/OneSignalSDK.page.js"></script>
<script>
  window.OneSignalDeferred = window.OneSignalDeferred || [];

  var onesignalBtns = Array.prototype.slice.call(document.querySelectorAll(".onesignal-subscribe-btn"));
  var onesignalReady = false;
  var onesignalOptInPromise = null;

  function onesignalOptIn() {
    if (!onesignalOptInPromise) {
      onesignalOptInPromise = window.OneSignal.User.PushSubscription.optIn().then(
        function (result) { onesignalOptInPromise = null; return result; },
        function (err) { onesignalOptInPromise = null; throw err; }
      );
    }
    return onesignalOptInPromise;
  }

  function onesignalSetLabel(text) {
    onesignalBtns.forEach(function (btn) {
      var label = btn.querySelector(".btn-label");
      if (label) label.textContent = text;
      btn.title = text;
    });
  }

  function onesignalDisableAll() {
    onesignalBtns.forEach(function (btn) { btn.disabled = true; });
  }

  onesignalBtns.forEach(function (onesignalBtn) {
    onesignalBtn.addEventListener("click", function () {
      if (!onesignalReady || !window.OneSignal) {
        onesignalSetLabel("Le service de notifications n'a pas pu charger (bloqueur de pub ?)");
        return;
      }
      onesignalSetLabel("Activation…");
      window.OneSignal.Notifications.requestPermission()
        .then(function () {
          if (window.OneSignal.Notifications.permission) {
            return onesignalOptIn();
          }
        })
        .catch(function (err) {
          onesignalSetLabel("Erreur : " + (err && err.message ? err.message : String(err)));
        })
        .then(function () {
          setTimeout(function () {
            if (window.OneSignal.User.PushSubscription.optedIn) {
              onesignalSetLabel("Notifications activées ✓");
              onesignalDisableAll();
            } else if (window.OneSignal.Notifications.permission) {
              onesignalSetLabel("Permission accordée, inscription en cours…");
            } else {
              onesignalSetLabel(
                "Refusé (état navigateur : " + (window.Notification ? window.Notification.permission : "API absente") + ")"
              );
            }
          }, 800);
        });
    });
  });

  OneSignalDeferred.push(function (OneSignal) {
    var cleanup = "serviceWorker" in navigator
      ? navigator.serviceWorker.ready
          .then(function (registration) { return registration.pushManager.getSubscription(); })
          .then(function (existingSub) {
            if (existingSub) return existingSub.unsubscribe();
          })
          .catch(function () {})
      : Promise.resolve();

    cleanup.then(function () {
      return OneSignal.init({
        appId: "1f47412d-5688-4ce7-8288-df1ed44bdad3",
        serviceWorkerPath: "OneSignalSDKWorker.js",
      });
    }).then(function () {
      onesignalReady = true;

      function updateBtn() {
        if (OneSignal.User.PushSubscription.optedIn) {
          onesignalSetLabel("Notifications activées ✓");
          onesignalDisableAll();
        } else if (OneSignal.Notifications.permission) {
          onesignalSetLabel("Finalisation de l'abonnement…");
          onesignalOptIn().catch(function (err) {
            onesignalSetLabel("Erreur : " + (err && err.message ? err.message : String(err)));
          });
        }
      }
      updateBtn();

      OneSignal.Notifications.addEventListener("permissionChange", updateBtn);
      OneSignal.User.PushSubscription.addEventListener("change", updateBtn);
    }).catch(function (err) {
      onesignalSetLabel(
        "Erreur d'initialisation : " + (err && err.message ? err.message : String(err))
      );
      console.error("OneSignal init failed:", err);
    });
  });
</script>
<script>
  (function(){
    var KEY = "scenario_intro_seen";
    var banner = document.getElementById("intro-banner");
    if (!banner) return;
    try {
      if (!localStorage.getItem(KEY)) {
        banner.hidden = false;
        localStorage.setItem(KEY, "1");
      }
    } catch (e) {
    }
    var closeBtn = document.getElementById("intro-banner-close");
    if (closeBtn) {
      closeBtn.addEventListener("click", function(){
        banner.hidden = true;
      });
    }
  })();
</script>
<script>
  (function(){
    // Réaffiché à chaque nouveau récap (data-hebdo change chaque dimanche),
    // masqué dès que ce récap précis a été vu ou fermé — pas un "vu une
    // fois pour toutes" comme .intro-banner.
    var KEY = "scenario_weekly_seen";
    var banner = document.getElementById("weekly-banner");
    if (!banner) return;
    var hebdoDate = banner.dataset.hebdo;
    var alreadySeen = false;
    try {
      alreadySeen = localStorage.getItem(KEY) === hebdoDate;
    } catch (e) {
    }
    if (!alreadySeen) { banner.hidden = false; }
    function dismiss(){
      banner.hidden = true;
      try { localStorage.setItem(KEY, hebdoDate); } catch (e) {}
    }
    var closeBtn = document.getElementById("weekly-banner-close");
    if (closeBtn) { closeBtn.addEventListener("click", dismiss); }
    var link = document.getElementById("weekly-banner-link");
    if (link) { link.addEventListener("click", dismiss); }
  })();
</script>
</body>
</html>
